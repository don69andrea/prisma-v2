---
phase: 04-v4-4-rag-sentiment-planned
plan: 07
type: execute
wave: 6
depends_on: ["04-05", "04-06"]
files_modified:
  - scripts/compare_sentiment_backtest.py
  - backend/tests/integration/test_backtest_sentiment_comparison.py
  - docs/PRISMA_V4_FORTSCHRITT.md
autonomous: false
requirements: [REQ-4-10]
must_haves:
  truths:
    - "compare_sentiment_backtest.py runs walk-forward twice (SENTIMENT_ENABLED false vs true) and reports Sharpe/Calmar/MaxDD/Hit-Rate + vetoed-trade count (D-08)"
    - "Results are written honestly to docs/PRISMA_V4_FORTSCHRITT.md, including if sentiment hurts (D-08 Ehrlichkeits-Regel)"
    - "No threshold tuning is applied to make the backtest look good (D-08)"
    - "If sentiment does not improve Sharpe/Calmar AND reduce MaxDD, SENTIMENT_ENABLED=false stays default (D-08, C-04)"
  artifacts:
    - path: "scripts/compare_sentiment_backtest.py"
      provides: "2x walk-forward comparison script"
      contains: "run_walkforward"
    - path: "docs/PRISMA_V4_FORTSCHRITT.md"
      provides: "Honest sentiment-impact section"
      contains: "Sentiment"
  key_links:
    - from: "scripts/compare_sentiment_backtest.py"
      to: "backend/application/backtest/walkforward.py"
      via: "run_walkforward() called twice (positions zeroed where veto)"
      pattern: "run_walkforward"
---

<objective>
Build the D-08 honesty backtest: a script that runs walk-forward twice under identical
conditions (SENTIMENT_ENABLED=false baseline vs true), compares Sharpe/Calmar/MaxDD/
Hit-Rate and counts vetoed trades, then documents the result honestly in
docs/PRISMA_V4_FORTSCHRITT.md — including the case where sentiment hurts.

Purpose: Mirrors the Meta-Labeling honesty protocol. The deliverable is an honest
measurement and a documented decision, NOT a tuned result. The phase keeps
SENTIMENT_ENABLED=false default unless the backtest clearly justifies enabling it.
Output: comparison script (GREEN test), honest FORTSCHRITT section, and a human-verified backtest run.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-RESEARCH.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: compare_sentiment_backtest.py — 2x walk-forward comparison</name>
  <files>scripts/compare_sentiment_backtest.py, backend/tests/integration/test_backtest_sentiment_comparison.py</files>
  <read_first>
    - scripts/llm_smoke_judge.py (async main pattern, asyncio.run entry, sys.exit)
    - backend/application/backtest/walkforward.py (run_walkforward / run_walkforward_with_details signatures — accept a 0/1 positions pd.Series; no SENTIMENT_ENABLED param exists)
    - backend/tests/integration/test_backtest_sentiment_comparison.py (RED stub from 04-02 — contract)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-RESEARCH.md (Open Question 3 — recommended approach: zero positions where senti.veto, no change to walkforward.py)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (section "compare_sentiment_backtest.py")
  </read_first>
  <action>
    Create scripts/compare_sentiment_backtest.py with the async-main + asyncio.run pattern from llm_smoke_judge.py. Run run_walkforward() twice on identical data: (1) DISABLED — positions unchanged (baseline = V4-1/V4-2 signal); (2) ENABLED — positions zeroed where the sentiment veto fires and downside size-scaling applied per D-06, derived from stored agent_audit_trail senti_view (or a replay). This avoids modifying walkforward.py (Open Question 3). Compute and print Sharpe, Calmar, MaxDD, Hit-Rate for both, plus the count of vetoed trades. Do NOT tune any threshold to improve the ENABLED result (D-08). Make the comparison logic importable so the integration test can drive it with small synthetic series. Complete test_backtest_sentiment_comparison.py (04-02 stub) to assert both modes produce the four metrics and a vetoed-trade count on a synthetic fixture (REQ-4-10). Use Decimal where money is involved; UTC-aware datetimes; asyncio only.
  </action>
  <verify>
    <automated>pytest backend/tests/integration/test_backtest_sentiment_comparison.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/compare_sentiment_backtest.py` exists, contains `run_walkforward`, and parses without syntax error
    - the comparison function is importable and produces Sharpe/Calmar/MaxDD + vetoed-trade count for both modes
    - `backend/application/backtest/walkforward.py` is NOT modified (Open Question 3 — regression check: file unchanged)
    - `pytest backend/tests/integration/test_backtest_sentiment_comparison.py -x` exits 0
  </acceptance_criteria>
  <done>Script runs walk-forward twice and reports comparison metrics + vetoed count; integration test green; walkforward.py untouched.</done>
</task>

<task type="execute">
  <name>Task 2: Document honest sentiment impact in PRISMA_V4_FORTSCHRITT.md</name>
  <files>docs/PRISMA_V4_FORTSCHRITT.md</files>
  <read_first>
    - docs/PRISMA_V4_FORTSCHRITT.md (existing structure + how Meta-Labeling negative-finding was documented — match that honest tone)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md (D-08 Ehrlichkeits-Regel — exact decision rule)
  </read_first>
  <action>
    Add a V4-4 Sentiment section (German, per response language) recording the 2x walk-forward comparison: a Sharpe/Calmar/MaxDD/Hit-Rate table for DISABLED vs ENABLED, the vetoed-trade count, and the explicit decision per D-08: if sentiment improves Sharpe AND Calmar AND reduces MaxDD → recommend enabling; otherwise SENTIMENT_ENABLED=false stays default with an optional veto-only mode noted, and the negative/neutral finding is stated plainly (like Meta-Labeling). State explicitly that no thresholds were tuned to flatter the result (D-08). The numbers come from the Task 1 / human-verify run — record the real measured values, not placeholders. Never use the word "Capstone".
  </action>
  <verify>
    <automated># D-08 no-tuning constraint is human-enforced via the blocking checkpoint:human-verify in Task 3 — automated check cannot verify absence of over-optimization.
grep -c 'Sentiment' docs/PRISMA_V4_FORTSCHRITT.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/PRISMA_V4_FORTSCHRITT.md` contains a V4-4 Sentiment section with a DISABLED-vs-ENABLED metrics table
    - the documented decision matches the D-08 rule (enable only if Sharpe AND Calmar improve AND MaxDD drops)
    - the section states no thresholds were tuned
    - the word "Capstone" does not appear in the added section
  </acceptance_criteria>
  <done>FORTSCHRITT.md honestly records the sentiment backtest result and the resulting SENTIMENT_ENABLED default decision.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>compare_sentiment_backtest.py runs walk-forward twice and the FORTSCHRITT section is drafted. The actual backtest needs a live DB and (optionally) the CryptoPanic API, which CI cannot reach.</what-built>
  <how-to-verify>
    1. Activate the venv: `source /tmp/prisma-v2/venv/bin/activate`
    2. Confirm CryptoPanic connectivity once: `python -c "import httpx; r=httpx.get('https://cryptopanic.com/api/v1/posts/?auth_token=free&currencies=BTC&kind=news'); print(r.status_code, r.json()['results'][0]['title'])"` — expect 200 and a headline.
    3. Run the comparison against the live backtest DB: `python scripts/compare_sentiment_backtest.py` — expect a printed Sharpe/Calmar/MaxDD/Hit-Rate table for DISABLED vs ENABLED plus a vetoed-trade count.
    4. Confirm the real measured numbers were written into the V4-4 Sentiment section of `docs/PRISMA_V4_FORTSCHRITT.md` and that the documented enable/disable decision follows D-08.
    5. Confirm SENTIMENT_ENABLED default stays false unless the run clearly satisfies the D-08 rule.
  </how-to-verify>
  <resume-signal>Type "approved" once the backtest ran and the honest numbers + decision are recorded, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backtest result → production default | A measured result decides whether sentiment is enabled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | prompt injection via news content | not applicable | This plan contains no LLM call; the backtest script replays stored signals and does not invoke the language model. |
| T-4-02 | Tampering | CryptoPanic malformed JSON | not applicable | This plan contains no API call to CryptoPanic; it consumes already-stored agent_audit_trail data. |
| T-4-03 | Tampering | LLM score hallucination | not applicable | This plan contains no LLM call; all numeric values consumed here originate from deterministic Python computations in prior plans. |
| T-4-04 | Elevation of Privilege | vote stuffing inflating backtest | mitigate | Backtest thresholds (_VETO_SCORE_THRESHOLD, _FEAR_THRESHOLD) are hard-coded constants from CONTEXT.md D-05, not tuned during the backtest (D-08 no-tuning constraint). Blocking human checkpoint in Task 3 enforces honest reporting. |
| T-4-OPT | Repudiation | overfitting the backtest | mitigate | D-08 forbids threshold tuning; honest reporting required; SENTIMENT_ENABLED stays false unless the strict improvement rule is met. |
| T-4-SC | Tampering | npm/pip installs | accept | No new packages (RESEARCH Package Legitimacy Audit clean). |
</threat_model>

<verification>
- pytest backend/tests/integration/test_backtest_sentiment_comparison.py -q passes
- walkforward.py unchanged (regression)
- FORTSCHRITT.md contains the honest comparison table and D-08 decision
</verification>

<success_criteria>
- 2x walk-forward comparison runs and reports the agreed metrics
- Honest result documented; no tuning
- SENTIMENT_ENABLED default decision follows D-08
</success_criteria>

<output>
Create `.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-07-SUMMARY.md` when done
</output>
