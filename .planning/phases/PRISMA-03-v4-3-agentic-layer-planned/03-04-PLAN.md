---
phase: 03-v4-3-agentic-layer
plan: 04
type: tdd
wave: 4
depends_on: ["03-01", "03-03"]
files_modified:
  - backend/application/agents/bull_research_agent.py
  - backend/application/agents/bear_research_agent.py
  - backend/application/agents/risk_agent.py
  - backend/infrastructure/llm/prompts/bull_research.de.md.j2
  - backend/infrastructure/llm/prompts/bear_research.de.md.j2
  - backend/infrastructure/llm/prompts/risk_agent.de.md.j2
  - backend/tests/unit/application/test_research_risk_agents.py
autonomous: true
requirements: [REQ-3.5, REQ-3.6, REQ-3.10]
must_haves:
  truths:
    - "Bull and Bear agents build one-sided cases via Claude Tool-Use API (forced tool_choice)"
    - "RiskAgent reads portfolio exposure from an injected Store/Tool, never from LLM memory"
    - "RiskAgent enforces no-shorting: SELL implies max_size 0.0, never negative"
  artifacts:
    - path: "backend/application/agents/bull_research_agent.py"
      provides: "BullResearchAgent.build_case(...) -> BullCase via tool-use"
      contains: "tool_choice"
    - path: "backend/application/agents/risk_agent.py"
      provides: "RiskAgent.assess(coin, engine_signal) -> RiskVerdict (exposure from Store)"
      contains: "class RiskAgent"
  key_links:
    - from: "bull_research_agent.py"
      to: "Anthropic Tool-Use API"
      via: "tools=[{input_schema: BullCase.model_json_schema()}], tool_choice forced"
      pattern: "model_json_schema"
    - from: "risk_agent.py"
      to: "portfolio exposure Store/repository"
      via: "injected repo read, asserted not from prompt memory"
      pattern: "exposure"
---

<objective>
Build the three Sonnet Tool-Use agents (D-01 LLM-loop side): BullResearchAgent and BearResearchAgent
produce deliberately one-sided cases, and RiskAgent issues a veto/cap. All three use the
UniverseSuggestionService Tool-Use pattern (`tools=[...]`, forced `tool_choice`, extract `block.input`
where `block.type == "tool_use"`). RiskAgent reads portfolio exposure from an injected Store/Tool —
NEVER from LLM memory (D-06 test 2) — and enforces no-shorting (D-06 test 7).

Purpose: This is the debate + risk layer. Bull/Bear are always both persisted later (minority
protection). RiskAgent is the safety gate.

Output: 3 agent modules + 3 prompts + unit tests.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-CONTEXT.md
@backend/application/services/universe_suggestion_service.py
@backend/domain/schemas/agent_schemas.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: BullResearchAgent + BearResearchAgent via Tool-Use (RED→GREEN)</name>
  <read_first>
    - backend/application/services/universe_suggestion_service.py (tools=, tool_choice, _extract_schema where block.type=="tool_use")
    - backend/infrastructure/llm/client.py (messages_create passes tools/tool_choice through sdk_kwargs)
    - backend/domain/schemas/agent_schemas.py (BullCase, BearCase)
  </read_first>
  <behavior>
    - Test: BullResearchAgent.build_case(tech, onchain, senti, macro, engine_signal) returns a valid BullCase extracted from a mocked tool_use block.
    - Test: BearResearchAgent.build_case(...) returns a valid BearCase.
    - Test (invalid output): LLM returns no tool_use block → InvalidLLMOutput / fallback handled, no crash.
    - Test: agent passes tools with input_schema == BullCase.model_json_schema() and tool_choice forcing the tool (assert on the kwargs the mocked LLMClient was called with).
  </behavior>
  <action>
    Create `BullResearchAgent` and `BearResearchAgent` mirroring UniverseSuggestionService: build
    `tools=[{"name": ..., "description": ..., "input_schema": BullCase.model_json_schema()}]`,
    `tool_choice={"type": "tool", "name": ...}`, call `messages_create` with Sonnet model
    (`claude-sonnet-4-6`), then extract the block where `block.type == "tool_use"` and
    `BullCase.model_validate(block.input)`. The Jinja2 prompts instruct each agent to argue ONLY its
    side (bull = strongest bullish thesis acknowledging risks; bear = strongest bearish thesis
    countering the bull). On missing tool_use, raise/handle a defined fallback case. Tests first.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/application/test_research_risk_agents.py -k "bull or bear" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Both agents return validated BullCase/BearCase from a mocked tool_use block
    - tool_choice forces the named tool (asserted on call kwargs)
    - Missing tool_use handled without uncaught exception
  </acceptance_criteria>
  <done>Bull and Bear agents produce one-sided cases via forced Tool-Use.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RiskAgent — exposure from Store + no-shorting (RED→GREEN)</name>
  <read_first>
    - backend/application/services/universe_suggestion_service.py (tool-use extraction)
    - backend/domain/schemas/agent_schemas.py (RiskVerdict)
    - 03-CONTEXT.md (D-06 test 2 State-from-Tool, test 7 No-Shorting)
  </read_first>
  <behavior>
    - Test (State-from-Tool / D-06 test 2): RiskAgent.assess reads exposure from a mocked Store returning a known value; assert the exposure used == Store value (not any LLM-provided number); inspect the rendered prompt to confirm the real exposure is injected from the tool, not hallucinated.
    - Test (No-Shorting / D-06 test 7): when engine_signal.action == "SELL", returned RiskVerdict.max_size == 0.0; assert max_size is never negative for any branch.
    - Test: RiskVerdict.approve False when a breach is present; breaches list populated.
    - Test (fallback): LLM Exception → conservative RiskVerdict (approve False or capped max_size), no raise.
  </behavior>
  <action>
    Create `RiskAgent` with constructor-injected portfolio-exposure Store/repository and LLMClient.
    `assess(coin, engine_signal)` first reads current exposure from the injected Store (await), passes
    that real number into the prompt context, then runs a Sonnet Tool-Use call producing RiskVerdict.
    Enforce post-LLM invariants in Python: if `engine_signal.action == "SELL"`, force
    `max_size = 0.0`; clamp max_size into [0.0, 1.5] (never negative). On Exception, return a
    conservative deterministic RiskVerdict. Tests first (RED), then implement.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/application/test_research_risk_agents.py -k "risk" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Exposure value used equals the mocked Store value (State-from-Tool asserted)
    - SELL → max_size == 0.0; no branch yields negative max_size
    - LLM Exception → conservative verdict, no raise
  </acceptance_criteria>
  <done>RiskAgent reads state from Store and enforces no-shorting; tests green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/application/test_research_risk_agents.py -q` green
</verification>

<success_criteria>
- Bull/Bear/Risk operate via Tool-Use; RiskAgent state-from-tool + no-shorting invariants hold (D-06 tests 2 & 7)
</success_criteria>

<output>
Create `.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-04-SUMMARY.md` when done
</output>
