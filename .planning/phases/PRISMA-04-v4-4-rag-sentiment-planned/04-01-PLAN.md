---
phase: 04-v4-4-rag-sentiment-planned
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/domain/entities/news_article.py
  - backend/domain/entities/news_retrieval_result.py
  - backend/infrastructure/persistence/repositories/news_repository.py
  - backend/domain/schemas/agent_schemas.py
  - backend/config.py
autonomous: true
requirements: [REQ-4-03, REQ-4-04, REQ-4-06, REQ-4-09]
must_haves:
  truths:
    - "A NewsArticle can be constructed with source='CRYPTOPANIC' without raising ValueError (D-07)"
    - "NewsRetrievalResult exposes a url field so SentimentView.sources can be populated (D-07)"
    - "find_nearest() returns nd.url and supports an optional 7-day TTL filter (D-02)"
    - "SentimentLLMOutput validates exactly news_surprise: bool + reasoning: str (D-04)"
    - "settings.sentiment_enabled reads SENTIMENT_ENABLED env var, default False (D-06)"
  artifacts:
    - path: "backend/domain/entities/news_article.py"
      provides: "Extended _VALID_SOURCES frozenset"
      contains: "CRYPTOPANIC"
    - path: "backend/domain/entities/news_retrieval_result.py"
      provides: "url field on NewsRetrievalResult"
      contains: "url: str"
    - path: "backend/infrastructure/persistence/repositories/news_repository.py"
      provides: "find_nearest selects nd.url + optional max_age_days TTL filter"
      contains: "nd.url"
    - path: "backend/domain/schemas/agent_schemas.py"
      provides: "SentimentLLMOutput Pydantic schema"
      contains: "class SentimentLLMOutput"
    - path: "backend/config.py"
      provides: "sentiment_enabled Settings field"
      contains: "sentiment_enabled"
  key_links:
    - from: "backend/infrastructure/persistence/repositories/news_repository.py"
      to: "backend/domain/entities/news_retrieval_result.py"
      via: "result construction maps url=str(row['url'])"
      pattern: "url=str\\(row\\[.url.\\]\\)"
---

<objective>
Land the foundational domain, repository, schema, and config changes that every
downstream wave depends on (Wave 0 blockers B-01, B-02, B-04-partial plus the new
SentimentLLMOutput schema and sentiment_enabled flag). These are interface contracts:
defining them first prevents downstream scavenger hunts.

Purpose: Without CRYPTOPANIC in _VALID_SOURCES, ingestion raises ValueError. Without
NewsRetrievalResult.url, SentimentView.sources cannot be populated. Without
SentimentLLMOutput, the agent has no Pydantic contract. Without sentiment_enabled,
the veto/size-scaling guard has nothing to read.
Output: Modified entities, repo SQL, schema, and config — all backward-compatible additive changes.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Extend _VALID_SOURCES + add NewsRetrievalResult.url field</name>
  <files>backend/domain/entities/news_article.py, backend/domain/entities/news_retrieval_result.py</files>
  <read_first>
    - backend/domain/entities/news_article.py (current _VALID_SOURCES = frozenset({"NZZ", "SRF"}) at line 10; __post_init__ validator at lines 25-28)
    - backend/domain/entities/news_retrieval_result.py (current dataclass fields, lines 11-22 — no url field)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (sections "news_article.py" and "news_retrieval_result.py" — exact diffs)
  </read_first>
  <behavior>
    - Test (news_article): NewsArticle(source="CRYPTOPANIC", ...) constructs without ValueError.
    - Test (news_article): NewsArticle(source="UNKNOWN", ...) still raises ValueError mentioning the allowed sources.
    - Test (news_article): existing sources "NZZ" and "SRF" still construct successfully (regression).
    - Test (news_retrieval_result): NewsRetrievalResult has a url field of type str; instantiation with url="https://x" succeeds and round-trips.
  </behavior>
  <action>
    RED: write/extend the failing tests described in <behavior> in the test stub files created by plan 04-02 (test_news_article.py, test_news_retrieval_result.py). If those stub files do not yet exist when this plan runs (waves run in parallel but 04-02 is same wave), create the minimal failing assertions inline here first, then reconcile.
    GREEN: In news_article.py change the constant to `_VALID_SOURCES = frozenset({"NZZ", "SRF", "CRYPTOPANIC"})` (per D-07, B-01). Do NOT touch the __post_init__ validator. In news_retrieval_result.py add field `url: str` immediately after `title: str` (per B-02). The dataclass is frozen — keep field ordering so positional construction in the repo stays valid; the repo update in Task 2 must pass url by keyword.
    REFACTOR: none expected — single-constant and single-field additive changes.
  </action>
  <verify>
    <automated>pytest backend/tests/unit/domain/entities/test_news_article.py backend/tests/unit/domain/entities/test_news_retrieval_result.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/domain/entities/news_article.py` contains `"CRYPTOPANIC"` inside `_VALID_SOURCES`
    - `backend/domain/entities/news_retrieval_result.py` contains `url: str`
    - `pytest backend/tests/unit/domain/entities/test_news_article.py backend/tests/unit/domain/entities/test_news_retrieval_result.py -x` exits 0
  </acceptance_criteria>
  <done>NewsArticle accepts CRYPTOPANIC; NewsRetrievalResult has url: str; existing sources still validate.</done>
</task>

<task type="execute">
  <name>Task 2: Add nd.url + optional 7-day TTL filter to find_nearest()</name>
  <files>backend/infrastructure/persistence/repositories/news_repository.py</files>
  <read_first>
    - backend/infrastructure/persistence/repositories/news_repository.py (find_nearest raw SQL at lines 93-110; result construction at lines 128-141)
    - backend/domain/entities/news_retrieval_result.py (the url field added in Task 1 — keyword url= must be supplied)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (section "news_repository.py" — exact SELECT and mapping diffs)
  </read_first>
  <action>
    Add `nd.url,` to the SELECT list immediately after `nd.title,` (B-02 fix per D-07). Add optional parameter `max_age_days: int | None = None` to the `find_nearest()` signature; when not None, append `AND nd.published_at > NOW() - INTERVAL ':max_age_days days'` to the WHERE clause using a SQLAlchemy bound parameter (NEVER string-concat the interval per AGENTS.md §8 — bind max_age_days as a parameter). When max_age_days is None, behavior is identical to today (backward compatible for NZZ/SRF — D-02 soft TTL). In the result construction, add `url=str(row["url"]),` mapped by keyword. Use parameterized queries only (AGENTS.md §7). This is glue/SQL wiring — covered by the retrieval integration test rather than a new unit test.
  </action>
  <verify>
    <automated>pytest backend/tests/unit/domain/entities/test_news_retrieval_result.py -x && python -c "import ast,sys; src=open('backend/infrastructure/persistence/repositories/news_repository.py').read(); assert 'nd.url' in src and 'max_age_days' in src; ast.parse(src)"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/infrastructure/persistence/repositories/news_repository.py` contains `nd.url` in the SELECT and `url=str(row["url"])` in result construction
    - `backend/infrastructure/persistence/repositories/news_repository.py` contains `max_age_days` parameter
    - file parses (no syntax error) via the python ast.parse check above
  </acceptance_criteria>
  <done>find_nearest() selects nd.url, maps it into NewsRetrievalResult, and accepts an optional max_age_days TTL filter using bound parameters.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Add SentimentLLMOutput schema + sentiment_enabled config flag</name>
  <files>backend/domain/schemas/agent_schemas.py, backend/config.py</files>
  <read_first>
    - backend/domain/schemas/agent_schemas.py (imports + __all__ at lines 1-20; SentimentView at lines 56-70 — FROZEN, do NOT modify; TechnicalView pattern lines 35-44)
    - backend/config.py (existing Settings simple-field pattern lines 17-54; get_settings lru_cache singleton lines 111-114)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (sections "agent_schemas.py" and "config.py")
  </read_first>
  <behavior>
    - Test (schema): SentimentLLMOutput(news_surprise=True, reasoning="x").news_surprise is True.
    - Test (schema): SentimentLLMOutput rejects a non-bool news_surprise (e.g. news_surprise="maybe") with ValidationError — Pydantic strict bool (T-4-03 / §0 Iron Rule: LLM produces no number).
    - Test (schema): SentimentLLMOutput has exactly the fields news_surprise and reasoning (no score/veto/regime field).
    - Test (config): with SENTIMENT_ENABLED unset, get_settings().sentiment_enabled is False.
    - Test (config): with SENTIMENT_ENABLED=true in env, a freshly-built Settings reads sentiment_enabled True (D-06 default-false guard).
  </behavior>
  <action>
    RED: write the failing tests above into test_agent_schemas.py and test_settings.py (stubs from plan 04-02).
    GREEN: In agent_schemas.py add `class SentimentLLMOutput(BaseModel)` with exactly two fields `news_surprise: bool` and `reasoning: str` (D-04, German docstring per PATTERNS); append "SentimentLLMOutput" to `__all__`. Do NOT modify SentimentView. In config.py add `sentiment_enabled: bool = False` to the Settings class after stale_batch_timeout_seconds (D-06, C-04 — default false stays default; pydantic-settings auto-reads SENTIMENT_ENABLED env var). Do not instantiate Settings() directly anywhere; the singleton get_settings() stays the access path (C-03).
    REFACTOR: none expected.
  </action>
  <verify>
    <automated>pytest backend/tests/unit/domain/schemas/test_agent_schemas.py backend/tests/unit/test_settings.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/domain/schemas/agent_schemas.py` contains `class SentimentLLMOutput` and `"SentimentLLMOutput"` appears in `__all__`
    - SentimentView definition in agent_schemas.py is unchanged (grep `class SentimentView` still present, same fields)
    - `backend/config.py` contains `sentiment_enabled: bool = False`
    - `pytest backend/tests/unit/domain/schemas/test_agent_schemas.py backend/tests/unit/test_settings.py -x` exits 0
  </acceptance_criteria>
  <done>SentimentLLMOutput validates news_surprise:bool + reasoning:str only; sentiment_enabled flag defaults False and reads SENTIMENT_ENABLED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM → application | LLM output crosses into Python; must be schema-validated |
| env → config | SENTIMENT_ENABLED env var crosses into Settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-03 | Tampering | SentimentLLMOutput | mitigate | Schema has only news_surprise: bool + reasoning: str; LLM can never emit a numeric score (§0 Iron Rule, D-04). Pydantic rejects non-bool news_surprise. |
| T-4-SC | Tampering | npm/pip installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: zero new deps; all pre-existing). |
</threat_model>

<verification>
- pytest backend/tests/unit/domain/ -q passes (entities + schemas)
- pytest backend/tests/unit/test_settings.py -q passes
- No modification to SentimentView schema (regression-grep)
</verification>

<success_criteria>
- CRYPTOPANIC accepted as a NewsArticle source
- NewsRetrievalResult.url present and populated by find_nearest()
- find_nearest() supports optional max_age_days TTL filter via bound parameter
- SentimentLLMOutput schema present and exported
- sentiment_enabled flag present, default False
</success_criteria>

<output>
Create `.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-01-SUMMARY.md` when done
</output>
