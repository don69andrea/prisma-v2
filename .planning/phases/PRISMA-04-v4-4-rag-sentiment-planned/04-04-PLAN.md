---
phase: 04-v4-4-rag-sentiment-planned
plan: 04
type: tdd
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - backend/application/services/news_ingestion_service.py
autonomous: true
requirements: [REQ-4-02]
must_haves:
  truths:
    - "NewsIngestionService.ingest_cryptopanic(coins) ingests CryptoPanic articles with source='CRYPTOPANIC' (D-07)"
    - "Chunk metadata carries votes_positive + votes_negative per article so D-03 scoring works (C-02)"
    - "Articles older than 7 days are skipped at ingestion (D-02 soft TTL)"
    - "Dedup via url_hash prevents re-embedding (D-02)"
  artifacts:
    - path: "backend/application/services/news_ingestion_service.py"
      provides: "ingest_cryptopanic() method + CryptoPanicAdapter injection"
      contains: "async def ingest_cryptopanic"
  key_links:
    - from: "backend/application/services/news_ingestion_service.py"
      to: "backend/infrastructure/adapters/cryptopanic_adapter.py"
      via: "self._cryptopanic.fetch_articles(coin)"
      pattern: "_cryptopanic\\.fetch_articles"
---

<objective>
Add ingest_cryptopanic(coins) to NewsIngestionService (TDD) as a separate method
alongside the existing RSS path — per B-03 we do NOT modify DEFAULT_FEEDS or the RSS
tuple structure. The new method dedups, applies the 7-day TTL, embeds, and crucially
stores votes in chunk metadata so the D-03 vote-ratio score has data (C-02).

Purpose: Bridges the adapter (04-03) into the existing pgvector corpus. Forgetting
votes-in-metadata silently degrades D-03 to F&G-only fallback (Pitfall 6).
Output: ingest_cryptopanic() method + CryptoPanicAdapter constructor injection.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md
@CLAUDE.md
@AGENTS.md
@backend/application/services/news_ingestion_service.py
@backend/infrastructure/adapters/cryptopanic_adapter.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: NewsIngestionService.ingest_cryptopanic() — TDD ingestion with votes in metadata</name>
  <files>backend/application/services/news_ingestion_service.py</files>
  <read_first>
    - backend/application/services/news_ingestion_service.py (existing _ingest_feed pattern lines 60-127; dedup lines 66-69; _chunk_text metadata lines 141-148; current constructor + DEFAULT_FEEDS lines 25-28 — do NOT change DEFAULT_FEEDS per B-03)
    - backend/infrastructure/adapters/cryptopanic_adapter.py (RawCryptoPanicArticle fields produced by 04-03)
    - backend/domain/entities/news_article.py (NewsArticle.hash_url + source validation now accepts CRYPTOPANIC after 04-01)
    - backend/tests/unit/application/test_news_ingestion_cryptopanic.py (RED stub from 04-02 — _build_service mock contract)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (section "news_ingestion_service.py" — ingest_cryptopanic structure + chunk metadata votes extension)
  </read_first>
  <behavior>
    - Test: ingest_cryptopanic(["BTC"]) with a fresh article calls repo.save_article once with source="CRYPTOPANIC" and tickers from currencies (no TickerNer).
    - Test: chunk metadata passed to save_chunks includes votes_positive and votes_negative (C-02) plus source="CRYPTOPANIC".
    - Test: a duplicate url_hash (exists_by_url_hash → True) increments skipped_duplicate and does not re-embed (D-02).
    - Test: an article with published_at older than 7 days is skipped (D-02 TTL); a 1-day-old article is ingested.
    - Test: an adapter/embedding exception for one article increments errors and does not abort the loop.
  </behavior>
  <action>
    RED: run test_news_ingestion_cryptopanic.py (04-02 stub) to confirm the missing method fails; complete the behavior tests above.
    GREEN: Add optional constructor param `cryptopanic_adapter: CryptoPanicAdapter | None = None` stored as `self._cryptopanic` (PATTERNS) — keep all existing params and DEFAULT_FEEDS untouched (B-03). Implement `async def ingest_cryptopanic(self, coins: list[str]) -> dict[str, int]` mirroring _ingest_feed structure: iterate coins, call `self._cryptopanic.fetch_articles(coin)`, filter `published_at > now(UTC) - 7d` (D-02), dedup via `NewsArticle.hash_url(raw.url)` + `exists_by_url_hash` (D-02), build NewsArticle(source="CRYPTOPANIC", tickers=raw.currencies) (no TickerNer per D-07), save_article, build chunks and set chunk metadata = {"source":"CRYPTOPANIC", "tickers":tickers, "votes_positive":raw.votes_positive, "votes_negative":raw.votes_negative} (C-02, Pitfall 6), embed via self._llm.embed, save_chunks. Per-article try/except increments an errors counter (robustness). Return stats dict {ingested, skipped_duplicate, errors}. Use asyncio primitives only; UTC-aware now via datetime.now(UTC).
    REFACTOR: factor a shared `_persist_article_with_chunks(article, metadata)` helper if it reduces duplication with _ingest_feed without touching the RSS path's behavior.
  </action>
  <verify>
    <automated>pytest backend/tests/unit/application/test_news_ingestion_cryptopanic.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/application/services/news_ingestion_service.py` contains `async def ingest_cryptopanic`
    - DEFAULT_FEEDS definition is unchanged (still the two-tuple RSS list — B-03 honored)
    - the method builds chunk metadata containing `votes_positive` and `votes_negative` (grep confirms both tokens present)
    - `pytest backend/tests/unit/application/test_news_ingestion_cryptopanic.py -x` exits 0
  </acceptance_criteria>
  <done>ingest_cryptopanic ingests CRYPTOPANIC articles with votes in chunk metadata, applies 7-day TTL and url_hash dedup, and leaves the RSS path untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adapter → corpus | External article content is embedded and stored as RAG chunks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | news content → chunks | mitigate | Article text is stored as structured RAG chunks (data, not instructions); the LLM consuming them (plan 04-05) validates output via Pydantic. |
| T-4-02 | Tampering | CryptoPanic malformed JSON | not applicable | The adapter (plan 04-03) wraps all JSON parsing with try/except → []; this ingestion plan trusts only the already-parsed RawCryptoPanicArticle objects it receives. |
| T-4-03 | Tampering | LLM score hallucination | not applicable | This plan contains no LLM call; no numeric LLM output can be produced here. |
| T-4-04 | Elevation of Privilege | inflated votes in metadata | mitigate | Votes are stored as raw ints; the score that consumes them (plan 04-05) is capped [-1,1] with a hard-coded veto threshold. |
| T-4-SC | Tampering | npm/pip installs | accept | No new packages (RESEARCH Package Legitimacy Audit clean). |
</threat_model>

<verification>
- pytest backend/tests/unit/application/test_news_ingestion_cryptopanic.py -q passes
- DEFAULT_FEEDS unchanged (regression grep)
- Chunk metadata includes votes (Pitfall 6 guard)
</verification>

<success_criteria>
- ingest_cryptopanic() ingests CRYPTOPANIC articles with votes in metadata
- 7-day TTL + url_hash dedup enforced
- Existing RSS ingestion path untouched
</success_criteria>

<output>
Create `.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-04-SUMMARY.md` when done
</output>
