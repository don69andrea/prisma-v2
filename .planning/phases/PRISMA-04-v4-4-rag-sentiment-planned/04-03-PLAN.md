---
phase: 04-v4-4-rag-sentiment-planned
plan: 03
type: tdd
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - backend/infrastructure/adapters/cryptopanic_adapter.py
autonomous: true
requirements: [REQ-4-01]
must_haves:
  truths:
    - "CryptoPanicAdapter.fetch_articles(coin) returns list[RawCryptoPanicArticle] parsed from the free API JSON (D-01)"
    - "Adapter returns [] on malformed JSON instead of raising (T-4-02)"
    - "Adapter uses retry/backoff identical to RssNewsAdapter (CLAUDE.md retry pattern)"
  artifacts:
    - path: "backend/infrastructure/adapters/cryptopanic_adapter.py"
      provides: "CryptoPanicAdapter + RawCryptoPanicArticle dataclass"
      contains: "class CryptoPanicAdapter"
      min_lines: 60
  key_links:
    - from: "backend/infrastructure/adapters/cryptopanic_adapter.py"
      to: "cryptopanic.com/api/v1/posts/"
      via: "httpx GET with auth_token=free"
      pattern: "auth_token"
---

<objective>
Build the new CryptoPanicAdapter infrastructure adapter (TDD) that fetches the
CryptoPanic free-API news feed per coin and parses it into a frozen
RawCryptoPanicArticle dataclass carrying votes and currency tags (D-01).

Purpose: This is the data source for the entire News-RAG pipeline. Votes
(positive/negative) feed the deterministic D-03 score; currencies map directly to
tickers (no TickerNer). Parsing must be defensive — malformed JSON returns [] (T-4-02).
Output: backend/infrastructure/adapters/cryptopanic_adapter.py, fully unit-tested against a static JSON fixture.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-RESEARCH.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md
@CLAUDE.md
@AGENTS.md
@backend/infrastructure/adapters/rss_news_adapter.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: CryptoPanicAdapter.fetch_articles() — TDD JSON parsing</name>
  <files>backend/infrastructure/adapters/cryptopanic_adapter.py</files>
  <read_first>
    - backend/infrastructure/adapters/rss_news_adapter.py (GOLD STANDARD: imports/constants lines 1-21, RawArticle dataclass 24-31, fetch_articles retry loop 42-56, _fetch_raw 58-66, ParseError → [] handling 72-76)
    - backend/tests/unit/infrastructure/test_cryptopanic_adapter.py (RED stub from 04-02 — the contract this must satisfy)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-RESEARCH.md (Pattern 1 CryptoPanicAdapter; CryptoPanic API JSON response structure with votes/currencies/published_at)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (section "cryptopanic_adapter.py" — dataclass + retry diffs)
  </read_first>
  <behavior>
    - Test: fetch_articles("BTC") on the static fixture returns one RawCryptoPanicArticle with votes_positive=11, votes_negative=2, currencies==["BTC"], url and published_at set.
    - Test: votes subfield missing → votes_positive/votes_negative default to 0 (A2 safety).
    - Test: malformed/non-JSON response → returns [] (does not raise) (T-4-02).
    - Test: results list capped at 50 articles (_MAX_ARTICLES, D-02).
    - Test: currencies[].code list with multiple coins → currencies == ["BTC","ETH"].
  </behavior>
  <action>
    RED: confirm test_cryptopanic_adapter.py (04-02 stub) fails on the missing module; flesh out the behavior tests above if the stub is thin.
    GREEN: Create cryptopanic_adapter.py with `from __future__ import annotations`, module `_logger`, constants `_TIMEOUT = httpx.Timeout(10.0, connect=5.0)`, `_MAX_RETRIES = 3`, `_RETRY_BACKOFF = [1.0, 2.0, 4.0]`, `_BASE_URL = "https://cryptopanic.com/api/v1/posts/"`, `_MAX_ARTICLES = 50`. Define `@dataclass(frozen=True) RawCryptoPanicArticle` with fields url, title, published_at (datetime|None), votes_positive:int, votes_negative:int, currencies:list[str]. Implement `async def fetch_articles(self, coin: str) -> list[RawCryptoPanicArticle]` using params {auth_token:"free", currencies:coin, kind:"news", public:"true"} (D-01) and the RssNewsAdapter retry loop verbatim. `_fetch_raw` uses resp.json() (not resp.text); wrap json access in try/except (json.JSONDecodeError, httpx.DecodingError) → return [] (T-4-02). Parse votes via votes.get("positive",0)/votes.get("negative",0); currencies via [c["code"] for c in results entry]; published_at parsed to UTC-aware datetime (CLAUDE.md: never naive). Cap to _MAX_ARTICLES. Use asyncio.sleep for backoff (never time.sleep). No run_in_executor.
    REFACTOR: extract a module-level `_parse_response(raw_json) -> list[RawCryptoPanicArticle]` if the fetch method exceeds one screen.
  </action>
  <verify>
    <automated>pytest backend/tests/unit/infrastructure/test_cryptopanic_adapter.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/infrastructure/adapters/cryptopanic_adapter.py` contains `class CryptoPanicAdapter` and `class RawCryptoPanicArticle`
    - file contains `auth_token` and `_RETRY_BACKOFF = [1.0, 2.0, 4.0]`
    - file contains no `run_in_executor` and no `time.sleep` (grep -v '^#' returns 0 matches)
    - `pytest backend/tests/unit/infrastructure/test_cryptopanic_adapter.py -x` exits 0
  </acceptance_criteria>
  <done>fetch_articles parses votes + currencies, caps at 50, returns [] on malformed JSON, retries with the standard backoff, all UTC-aware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CryptoPanic API → adapter | Untrusted external JSON crosses into the application |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | prompt injection via news content | not applicable | This adapter has no LLM call; fetched article text is stored as structured data, not passed to a model here. |
| T-4-02 | Tampering | CryptoPanic JSON parse | mitigate | resp.json() wrapped in try/except (JSONDecodeError, DecodingError) → returns []; votes.get(...,0) handles missing subfields. |
| T-4-03 | Tampering | LLM score hallucination | not applicable | This plan contains no LLM call; no numeric LLM output can be produced here. |
| T-4-04 | Elevation of Privilege | inflated CryptoPanic votes | mitigate | Adapter only carries raw vote ints; downstream D-03 score is capped to [-1,1] and the veto threshold is a hard-coded constant (plan 04-05). |
| T-4-SC | Tampering | npm/pip installs | accept | No new packages (httpx pre-existing; RESEARCH Package Legitimacy Audit clean). |
</threat_model>

<verification>
- pytest backend/tests/unit/infrastructure/test_cryptopanic_adapter.py -q passes
- grep confirms no run_in_executor / time.sleep in the adapter
- published_at values are timezone-aware
</verification>

<success_criteria>
- CryptoPanicAdapter parses the free-API JSON into RawCryptoPanicArticle
- Votes and currency tags extracted; malformed JSON yields []
- Retry/backoff matches the project standard
</success_criteria>

<output>
Create `.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-03-SUMMARY.md` when done
</output>
