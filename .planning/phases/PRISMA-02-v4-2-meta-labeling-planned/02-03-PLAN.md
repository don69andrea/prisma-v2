---
phase: 02-v4-2-meta-labeling-planned
plan: 03
type: tdd
wave: 3
depends_on: [02-02]
files_modified:
  - backend/tests/unit/application/test_walkforward.py
  - backend/application/backtest/walkforward.py
  - backend/interfaces/rest/schemas/signals.py
autonomous: true
requirements: [ML-07, ML-08]
must_haves:
  truths:
    - "run_walkforward and run_walkforward_with_details accept an optional meta_filter: pd.Series | None = None that is fully backward-compatible (no param = identical output)"
    - "when meta_filter is provided, positions are masked to 0 wherever meta_filter == 0"
    - "always-trade baseline and meta-filtered strategy are evaluated on the same OOS dates (ML-07)"
    - "MetaLabelReport Pydantic schema exists with the exact fields from CONTEXT.md"
  artifacts:
    - path: "backend/application/backtest/walkforward.py"
      provides: "meta_filter parameter on both walk-forward entry points"
      contains: "meta_filter"
    - path: "backend/interfaces/rest/schemas/signals.py"
      provides: "MetaLabelReport schema"
      contains: "class MetaLabelReport"
    - path: "backend/tests/unit/application/test_walkforward.py"
      provides: "meta_filter integration + backward-compat tests (ML-07, ML-08)"
      contains: "meta_filter"
  key_links:
    - from: "backend/application/backtest/walkforward.py"
      to: "meta_filter series (from predict_meta_label)"
      via: "position = position * meta_filter.reindex(close.index).fillna(0)"
      pattern: "meta_filter"
    - from: "backend/interfaces/rest/schemas/signals.py (MetaLabelReport)"
      to: "API endpoint (Wave D)"
      via: "response_model for GET /api/v1/signals/meta-label/{coin}"
      pattern: "class MetaLabelReport"
---

<objective>
Wave C — Backtest Integration. Extend the walk-forward engine with an optional, non-breaking meta_filter parameter and add the MetaLabelReport Pydantic contract. Test-First: backward-compat and baseline-comparison tests are RED before the engine change.

Purpose: This wires the meta-classifier output into the real backtest so "meta-filtered" can be compared against "always-trade" on the identical OOS period (D Baseline Comparison). The extension must be backward-compatible (Pitfall 5) — every existing test_walkforward.py caller passes nothing and sees identical behavior. Per D (Pydantic Schemas) MetaLabelReport carries both baseline and filtered Sharpe/Calmar plus the finding verdict.
Output: meta_filter on both walk-forward functions; MetaLabelReport schema; ML-07, ML-08 green; existing walkforward suite still green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-CONTEXT.md
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-RESEARCH.md
@backend/application/backtest/walkforward.py
@backend/interfaces/rest/schemas/signals.py
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-02-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — meta_filter backward-compat + baseline-comparison tests (ML-07, ML-08)</name>
  <files>backend/tests/unit/application/test_walkforward.py</files>
  <read_first>
    - backend/tests/unit/application/test_walkforward.py (existing fixtures + call sites to extend)
    - backend/application/backtest/walkforward.py (current signatures of run_walkforward / run_walkforward_with_details)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-CONTEXT.md (Baseline Comparison; Test Strategy items 7, 8)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-RESEARCH.md (Pattern 4; Pitfall 5)
  </read_first>
  <behavior>
    - test_meta_filter_backward_compat (ML-08): calling run_walkforward_with_details WITHOUT meta_filter returns byte-identical metrics to the current implementation (default None = no change); calling with meta_filter=all-ones equals the no-filter result.
    - test_meta_filter_masks_positions: meta_filter with some zeros reduces n_trades and zeroes the position on those dates.
    - test_baseline_same_oos_period (ML-07): always-trade (no filter) and meta-filtered details are computed over the same net_returns index (same OOS dates) — assert index equality of the comparable return series.
  </behavior>
  <action>
    Extend the existing test file (preserve all current tests). Add tests that pass `meta_filter` into run_walkforward_with_details. For ML-08, compute details once with no arg and once with `meta_filter=pd.Series(1.0, index=close.index)` and assert the strategy metrics match (atol 1e-12). For the masking test, build a meta_filter with a block of zeros and assert n_trades and exposure drop versus the unfiltered run. For ML-07, assert the always-trade and meta-filtered runs share the same return index. Keep <=100 chars/line. Run → RED (TypeError: unexpected keyword 'meta_filter'). Commit `test(walkforward): add RED meta_filter + baseline tests (ML-07, ML-08)`.
  </action>
  <verify>
    <automated>cd /Users/andreapetretta/prisma-v2 && pytest backend/tests/unit/application/test_walkforward.py -q -k "meta_filter or baseline_same_oos" 2>&1 | grep -qiE "error|failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Tests test_meta_filter_backward_compat, test_meta_filter_masks_positions, test_baseline_same_oos_period present.
    - Suite RED because meta_filter is not yet a parameter.
    - Pre-existing test_walkforward.py tests unaffected (they still pass under current engine).
  </acceptance_criteria>
  <done>Three new RED tests target the not-yet-existing meta_filter parameter.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — add meta_filter param + MetaLabelReport schema</name>
  <files>backend/application/backtest/walkforward.py, backend/interfaces/rest/schemas/signals.py</files>
  <read_first>
    - backend/tests/unit/application/test_walkforward.py (ML-07, ML-08 + masking expectations)
    - backend/application/backtest/walkforward.py (position = signals.shift(1)... mask insertion point after line ~162)
    - backend/interfaces/rest/schemas/signals.py (BacktestReport style: __all__, Literal, BaseModel)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-CONTEXT.md (Pydantic Schemas — exact MetaLabelReport fields)
  </read_first>
  <action>
    walkforward.py: add `meta_filter: pd.Series | None = None` as the LAST keyword to BOTH run_walkforward and run_walkforward_with_details (default None keeps existing callers working — Pitfall 5). Pass meta_filter through from run_walkforward into run_walkforward_with_details. Inside run_walkforward_with_details, immediately after `position = signals.shift(1).fillna(0.0).clip(...)`, if meta_filter is not None: `meta_aligned = meta_filter.reindex(close.index).fillna(0.0)` then `position = position * meta_aligned` (mask to 0 where filter is 0). Everything downstream (turnover, costs, baseline, metrics) recomputes from the masked position automatically, so the baseline is exposure-matched to the filtered strategy over the same dates.

    schemas/signals.py: add `MetaLabelReport(BaseModel)` with the EXACT fields from CONTEXT.md (D Pydantic Schemas): coin: str; label_method: Literal["triple_barrier","trend_scan"]; classifier: Literal["logreg","lgbm"]; n_folds: int; oos_precision: float; oos_recall: float; always_trade_sharpe: float; always_trade_calmar: float; meta_filtered_sharpe: float; meta_filtered_calmar: float; n_trades_always: int; n_trades_filtered: int; beats_baseline: bool; finding: Literal["positive","secondary_pass","negative"]; finding_reason: str. Add "MetaLabelReport" to __all__.

    mypy strict; <=100 chars/line. Run → GREEN. Commit `feat(walkforward): add optional meta_filter + MetaLabelReport schema`.
  </action>
  <verify>
    <automated>cd /Users/andreapetretta/prisma-v2 && pytest backend/tests/unit/application/test_walkforward.py -q && python -c "from backend.interfaces.rest.schemas.signals import MetaLabelReport"</automated>
  </verify>
  <acceptance_criteria>
    - run_walkforward and run_walkforward_with_details both accept meta_filter (no TypeError); omitting it reproduces prior metrics exactly (ML-08).
    - meta_filter zeros mask positions and lower n_trades; always-trade vs filtered share the same OOS index (ML-07).
    - MetaLabelReport importable with all 15 fields; Literal constraints enforced.
    - Full existing walkforward suite still green (no regression).
    - `ruff check backend/ && mypy backend/application/backtest/walkforward.py backend/interfaces/rest/schemas/signals.py` pass.
  </acceptance_criteria>
  <done>ML-07, ML-08 green; meta_filter non-breaking; MetaLabelReport schema present; existing walkforward tests still pass.</done>
</task>

</tasks>

<artifacts_produced>
New symbols introduced this plan:
- `meta_filter: pd.Series | None = None` parameter on run_walkforward and run_walkforward_with_details in backend/application/backtest/walkforward.py
- `MetaLabelReport(BaseModel)` in backend/interfaces/rest/schemas/signals.py (15 fields per CONTEXT.md)
- Tests: test_meta_filter_backward_compat, test_meta_filter_masks_positions, test_baseline_same_oos_period
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| meta_filter series → position masking | In-process numeric series aligned by index; reindex+fillna(0) coerces gaps to "no trade" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | breaking existing run_walkforward callers | mitigate | meta_filter defaults to None (non-breaking); full existing test_walkforward.py suite must stay green (ML-08) |
| T-02-06 | Repudiation | cherry-picked OOS comparison | mitigate | baseline and filtered evaluated on the same masked-position index; ML-07 asserts identical OOS dates |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest backend/tests/unit/application/test_walkforward.py -q` green including all pre-existing tests.
- `ruff check backend/ && ruff format --check backend/ && mypy backend/application/backtest/walkforward.py backend/interfaces/rest/schemas/signals.py` all pass.
</verification>

<success_criteria>
meta_filter is optional and non-breaking; positions mask to 0 on filter==0; always-trade and meta-filtered share the same OOS dates; MetaLabelReport matches the CONTEXT.md contract exactly; TDD order preserved.
</success_criteria>

<output>
Create `.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-03-SUMMARY.md` when done
</output>
