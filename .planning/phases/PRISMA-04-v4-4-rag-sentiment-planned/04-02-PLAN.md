---
phase: 04-v4-4-rag-sentiment-planned
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0042_widen_news_source_column.py
  - backend/tests/unit/domain/entities/test_news_article.py
  - backend/tests/unit/domain/entities/test_news_retrieval_result.py
  - backend/tests/unit/domain/schemas/test_agent_schemas.py
  - backend/tests/unit/test_settings.py
  - backend/tests/unit/application/test_sentiment_score_formula.py
  - backend/tests/unit/infrastructure/test_cryptopanic_adapter.py
  - backend/tests/unit/application/test_news_ingestion_cryptopanic.py
  - backend/tests/integration/test_backtest_sentiment_comparison.py
autonomous: true
requirements: [REQ-4-01, REQ-4-02, REQ-4-03, REQ-4-04, REQ-4-06, REQ-4-07, REQ-4-09, REQ-4-10]
must_haves:
  truths:
    - "Migration 0042 widens news_documents.source from VARCHAR(10) to VARCHAR(20) so CRYPTOPANIC (11 chars) fits (B-04, D-07)"
    - "All Wave-0 RED test stubs exist so every downstream task has a failing automated check before implementation (Nyquist)"
  artifacts:
    - path: "backend/alembic/versions/0042_widen_news_source_column.py"
      provides: "Column-widening migration"
      contains: "alter_column"
    - path: "backend/tests/unit/application/test_sentiment_score_formula.py"
      provides: "8 boundary-value RED stubs for D-03 score + D-05 veto"
      contains: "parametrize"
    - path: "backend/tests/unit/infrastructure/test_cryptopanic_adapter.py"
      provides: "RED stub for adapter JSON parsing"
      contains: "fetch_articles"
  key_links:
    - from: "backend/alembic/versions/0042_widen_news_source_column.py"
      to: "backend/alembic/versions/0041_agent_audit_trail.py"
      via: "down_revision = 0041"
      pattern: "down_revision.*0041"
---

<objective>
Land the column-widening migration (blocker B-04) and create every Wave-0 RED test
stub identified in 04-VALIDATION.md so downstream TDD tasks inherit a failing automated
check (Nyquist rule). This plan owns only the migration file and test files — zero
overlap with 04-01's source files, so both run in parallel in Wave 1.

Purpose: "CRYPTOPANIC" is 11 chars; the source column is VARCHAR(10) — inserts fail
without 0042. The test stubs make RED real before GREEN implementation in later waves.
Output: Migration 0042 + 8 RED test files (xfail/skeleton assertions referencing not-yet-existing symbols).
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-VALIDATION.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="execute">
  <name>Task 1: Alembic migration 0042 — widen news_documents.source to VARCHAR(20)</name>
  <files>backend/alembic/versions/0042_widen_news_source_column.py</files>
  <read_first>
    - backend/alembic/versions/0041_agent_audit_trail.py (boilerplate: revision/down_revision/branch_labels/depends_on at lines 14-24; upgrade/downgrade shape)
    - backend/alembic/versions/0014_create_news_tables.py (original source VARCHAR(10) definition — confirm current type and nullability before altering)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (section "0042_widen_news_source_column.py" — exact alter_column DDL)
  </read_first>
  <action>
    Create migration with `revision = "0042"`, `down_revision = "0041"`, branch_labels/depends_on = None. upgrade(): `op.alter_column("news_documents", "source", type_=sa.String(20), existing_type=sa.String(10), nullable=False)` (B-04, D-07). downgrade(): reverse to String(10). If 04-RESEARCH/migration 0014 shows news_chunks also has a source column of VARCHAR(10), widen it identically in the same migration; otherwise leave news_chunks untouched (confirm via read_first). DDL only — verified at migration time, no unit test.
  </action>
  <verify>
    <automated>python -c "import ast; src=open('backend/alembic/versions/0042_widen_news_source_column.py').read(); ast.parse(src); assert 'alter_column' in src and '\"0042\"' in src and '\"0041\"' in src and 'String(20)' in src"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/alembic/versions/0042_widen_news_source_column.py` contains `revision` = "0042" and `down_revision` = "0041"
    - file contains `alter_column` widening source to `String(20)`
    - file parses without syntax error
  </acceptance_criteria>
  <done>Migration 0042 exists, chains from 0041, widens source to VARCHAR(20) with a working downgrade.</done>
</task>

<task type="execute">
  <name>Task 2: Create Wave-0 RED test stubs (domain, schema, config, score formula)</name>
  <files>backend/tests/unit/domain/entities/test_news_article.py, backend/tests/unit/domain/entities/test_news_retrieval_result.py, backend/tests/unit/domain/schemas/test_agent_schemas.py, backend/tests/unit/test_settings.py, backend/tests/unit/application/test_sentiment_score_formula.py</files>
  <read_first>
    - backend/tests/unit/application/test_analyst_agents.py (header pattern lines 1-21; TestSentimentAnalystAgent builder lines 248-263; parametrized formula test lines 283-291)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-VALIDATION.md (Wave 0 Requirements list + Per-Task Verification Map)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md (D-03 score formula, D-05 veto truth table)
    - backend/domain/entities/news_article.py (existing test target — for test_news_article.py modify-or-create)
  </read_first>
  <action>
    Create each file with `from __future__ import annotations` and `pytestmark = pytest.mark.unit`.
    test_news_article.py: assert NewsArticle accepts source="CRYPTOPANIC" (REQ-4-03), rejects "UNKNOWN", accepts NZZ/SRF. If a test file already exists for this entity, extend it rather than overwrite.
    test_news_retrieval_result.py: assert NewsRetrievalResult has url:str field and round-trips (REQ-4-04).
    test_agent_schemas.py: assert SentimentLLMOutput(news_surprise:bool, reasoning:str) validates and rejects non-bool news_surprise; assert exactly two fields (REQ-4-06).
    test_settings.py: assert get_settings().sentiment_enabled is False by default and reads SENTIMENT_ENABLED=true (REQ-4-09).
    test_sentiment_score_formula.py: @pytest.mark.parametrize the D-03 blend formula score = 0.7*(pos-neg)/max(1,pos+neg) + 0.3*(fg-50)/50 for >=5 articles, the fallback score=fg_norm for <5 articles, regime boundaries at -0.2/+0.2, and the D-05 veto truth table — all 8 combinations of (regime FEAR vs not, news_surprise True vs False, score<-0.3 vs >=-0.3). These reference SentimentAnalystAgent symbols that do not exist yet, so the file must RED-fail at import/collection — acceptable for Wave-0 stub. Use the constants _VETO_SCORE_THRESHOLD=-0.3, _FEAR_THRESHOLD=-0.2, _MIN_ARTICLES_FOR_VOTE_RATIO=5.
    These are stub files: they MUST fail now (RED) and turn GREEN when the corresponding implementation lands in plans 04-01/04-05.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['backend/tests/unit/domain/entities/test_news_article.py','backend/tests/unit/domain/entities/test_news_retrieval_result.py','backend/tests/unit/domain/schemas/test_agent_schemas.py','backend/tests/unit/test_settings.py','backend/tests/unit/application/test_sentiment_score_formula.py']]"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist, parse without syntax error, and contain `pytestmark = pytest.mark.unit`
    - test_sentiment_score_formula.py contains `parametrize` and the three thresholds (-0.3, -0.2, 5)
    - test_news_article.py asserts a `CRYPTOPANIC` source case
    - Running `pytest backend/tests/unit/application/test_sentiment_score_formula.py -x` exits non-zero (RED — implementation absent), proving the failing check exists
  </acceptance_criteria>
  <done>Five RED test stubs exist and fail for the right reason (missing implementation), ready to go GREEN downstream.</done>
</task>

<task type="execute">
  <name>Task 3: Create Wave-1/2/3 RED test stubs (adapter, ingestion, backtest)</name>
  <files>backend/tests/unit/infrastructure/test_cryptopanic_adapter.py, backend/tests/unit/application/test_news_ingestion_cryptopanic.py, backend/tests/integration/test_backtest_sentiment_comparison.py</files>
  <read_first>
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (sections "test_cryptopanic_adapter.py", "test_news_ingestion_cryptopanic.py", "test_backtest_sentiment_comparison.py" — exact mock/builder patterns)
    - backend/tests/integration/test_agent_mandatory_suite.py (integration header + _make_director factory)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-RESEARCH.md (CryptoPanic API JSON shape; votes.positive/negative; currencies[].code)
  </read_first>
  <action>
    test_cryptopanic_adapter.py (pytest.mark.unit): mock httpx AsyncClient.get returning the static CryptoPanic JSON fixture from PATTERNS; assert fetch_articles("BTC") parses votes_positive=11, votes_negative=2, currencies==["BTC"] (REQ-4-01); assert malformed JSON → returns [] (T-4-02).
    test_news_ingestion_cryptopanic.py (pytest.mark.unit): use the _build_service mock pattern from PATTERNS; assert ingest_cryptopanic(["BTC"]) calls save_article with source="CRYPTOPANIC" and that chunk metadata carries votes_positive/votes_negative (REQ-4-02, C-02); assert >7d-old articles are skipped (D-02 TTL).
    test_backtest_sentiment_comparison.py (pytest.mark.integration): assert the comparison entrypoint produces Sharpe/Calmar/MaxDD for both SENTIMENT_ENABLED modes and a vetoed-trade count (REQ-4-10).
    All three reference symbols not yet implemented → RED at collection/run. Stubs only.
  </action>
  <verify>
    <automated>python -c "import ast; [ast.parse(open(f).read()) for f in ['backend/tests/unit/infrastructure/test_cryptopanic_adapter.py','backend/tests/unit/application/test_news_ingestion_cryptopanic.py','backend/tests/integration/test_backtest_sentiment_comparison.py']]"</automated>
  </verify>
  <acceptance_criteria>
    - All 3 files exist, parse, and carry the correct `pytestmark` (unit/unit/integration)
    - test_cryptopanic_adapter.py asserts votes_positive/votes_negative parsing and an empty-list-on-malformed-JSON case
    - test_news_ingestion_cryptopanic.py asserts source="CRYPTOPANIC" and votes in chunk metadata
    - `pytest backend/tests/unit/infrastructure/test_cryptopanic_adapter.py -x` exits non-zero (RED)
  </acceptance_criteria>
  <done>Three RED stubs exist for adapter, ingestion, and backtest, each failing due to missing implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → DB | DDL alters production schema column width |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Tampering | CryptoPanic JSON parse | mitigate | Adapter stub test asserts malformed JSON → returns [] (verified in plan 04-03 GREEN). |
| T-4-DB | Denial of Service | source column overflow | mitigate | Migration 0042 widens VARCHAR(10)→VARCHAR(20) before any CRYPTOPANIC insert; prevents DataError. |
| T-4-SC | Tampering | npm/pip installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit: zero new deps). |
</threat_model>

<verification>
- All 8 test stub files parse and are collectable by pytest
- Migration 0042 parses and chains from 0041
- Score-formula and adapter stubs are RED (non-zero exit) confirming missing implementation
</verification>

<success_criteria>
- Migration 0042 present and revision-chained
- All Wave-0 RED test stubs from 04-VALIDATION.md exist
- Every downstream requirement (REQ-4-01..10 minus 05/08 which extend existing files) has a failing automated check
</success_criteria>

<output>
Create `.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-02-SUMMARY.md` when done
</output>
