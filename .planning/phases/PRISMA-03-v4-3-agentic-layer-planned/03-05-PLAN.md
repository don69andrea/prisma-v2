---
phase: 03-v4-3-agentic-layer
plan: 05
type: tdd
wave: 5
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - backend/application/agents/signal_director.py
  - backend/tests/unit/application/test_signal_director.py
autonomous: true
requirements: [REQ-3.7, REQ-3.10]
must_haves:
  truths:
    - "SignalDirector orchestrates engine + 4 analysts (asyncio.gather) + Bull/Bear/Risk sequentially → TradeSignal"
    - "On any analyst LLM Exception the pipeline still returns a TradeSignal from engine_signal with lowered confidence (D-06 test 4)"
    - "confidence < 0.65 triggers exactly one HITL checkpoint (non-blocking log + disclaimer update, D-06 test 6)"
    - "Every run persists Bull AND Bear cases to the audit trail (minority protection, D-06 test 3)"
  artifacts:
    - path: "backend/application/agents/signal_director.py"
      provides: "SignalDirector.run(coin) -> TradeSignal; Python weighted synthesis"
      contains: "class SignalDirector"
  key_links:
    - from: "signal_director.py"
      to: "AgentAuditTrailRepository.insert"
      via: "persist all agent outputs, embed returned UUID in TradeSignal.audit_trail_id"
      pattern: "insert"
    - from: "signal_director.py"
      to: "asyncio.gather"
      via: "parallel analyst invocation with return_exceptions=True"
      pattern: "asyncio.gather"
---

<objective>
Build the orchestration hub (D-01 synthesis side, D-07 pipeline core): `SignalDirector.run(coin)` runs
the deterministic engine, the 4 analysts in parallel via `asyncio.gather(return_exceptions=True)`, then
Bull/Bear/Risk sequentially, performs a Python-level weighted synthesis into `TradeSignal`, fires a
HITL checkpoint when confidence < 0.65, and persists the full agent run to the audit trail (Bull AND
Bear both stored → minority protection).

Purpose: This is where the hybrid orchestration, fallback resilience, HITL checkpoint, and audit-trail
linkage converge. Covers D-06 tests 3, 4, 6.

Output: signal_director.py + unit tests for synthesis, fallback, checkpoint, minority-protection.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-CONTEXT.md
@./CLAUDE.md
@backend/domain/schemas/agent_schemas.py
@backend/application/signals/signal_service.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: SignalDirector orchestration + Python synthesis (RED→GREEN)</name>
  <read_first>
    - 03-CONTEXT.md (D-01 run() pseudocode, D-07 pipeline)
    - ./CLAUDE.md (asyncio.to_thread for sync engine calls, NOT run_in_executor)
    - backend/application/signals/signal_service.py (evaluate signature for engine_signal)
    - backend/domain/schemas/agent_schemas.py (TradeSignal, all view types)
    - backend/application/agents/risk_agent.py (assess signature)
  </read_first>
  <behavior>
    - Test: run(coin) returns a valid TradeSignal whose action mirrors the engine action and whose audit_trail_id is the UUID returned by a mocked repo insert.
    - Test (synthesis): confidence is a weighted Python combination of analyst/bull/bear/risk confidences (deterministic, not LLM free-form); rationale_by_layer has keys technical, onchain, sentiment, macro, bull, bear, risk.
    - Test (no-shorting propagation): RiskVerdict.max_size 0.0 → TradeSignal.size_factor 0.0.
  </behavior>
  <action>
    Create `SignalDirector` with constructor injection of: engine signal service, the 4 analyst agents,
    Bull/Bear/Risk agents, and AgentAuditTrailRepository. `async def run(self, coin) -> TradeSignal`
    follows the D-01 sequence: (1) `engine_signal = await ...evaluate(coin)` (wrap sync via
    asyncio.to_thread per CLAUDE.md); (2) `tech, onchain, senti, macro = await asyncio.gather(...,
    return_exceptions=True)`; (3) sequential `bull`, `bear`, `risk`; (4) Python `_synthesize(...)`
    building TradeSignal with weighted confidence and rationale_by_layer dict; clamp size_factor to
    RiskVerdict.max_size (no-shorting propagation). Do the audit-trail persist + checkpoint in Task 2.
    Tests first.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/application/test_signal_director.py -k "synthesis or run or shorting" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - run(coin) returns valid TradeSignal; audit_trail_id == mocked insert UUID
    - rationale_by_layer contains all 7 layer keys
    - RiskVerdict.max_size 0.0 → size_factor 0.0
  </acceptance_criteria>
  <done>Director orchestrates engine + agents and synthesizes TradeSignal in Python.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Fallback, HITL checkpoint, audit-trail persist (RED→GREEN)</name>
  <read_first>
    - 03-CONTEXT.md (D-06 tests 3, 4, 6)
    - 03-RESEARCH.md (checkpoint = log warning + disclaimer update, non-blocking, mock logging.warning assert call_count==1)
    - backend/infrastructure/persistence/repositories/agent_audit_trail_repository.py (insert signature)
  </read_first>
  <behavior>
    - Test (Fallback / D-06 test 4): an analyst in asyncio.gather raises Exception → run() still returns a TradeSignal derived from engine_signal, confidence lowered, disclaimer set; no exception propagates.
    - Test (Checkpoint / D-06 test 6): confidence < 0.65 → exactly one HITL call (mock logging.warning / checkpoint hook, assert call_count == 1) and disclaimer updated with a LOW CONFIDENCE prefix; checkpoint is non-blocking (no interactive UI).
    - Test (Minority-Protection / D-06 test 3): scenario 1 strong Bear vs 3 Bulls → the agent_run dict passed to repo.insert CONTAINS the bear_case; RiskAgent can overrule (verdict respected in size_factor).
    - Test: repo.insert called exactly once per run; returned UUID embedded in TradeSignal.
  </behavior>
  <action>
    Extend `run()`: after synthesis, if any gather result `isinstance(result, Exception)`, substitute a
    deterministic fallback view and lower confidence (Fallback). Build the `agent_run` dict containing
    ALL eight outputs (tech, onchain, senti, macro, bull, bear, risk, trade_signal) and call
    `await repo.insert(coin, asof, agent_run)`; embed returned UUID in TradeSignal.audit_trail_id BEFORE
    returning (re-construct TradeSignal with the id). If `signal.confidence < 0.65`, call a non-blocking
    checkpoint: `logging.warning("LOW CONFIDENCE ...")` and set disclaimer to a "LOW CONFIDENCE: ..."
    prefixed string (UI is V4-5; do NOT block). Tests first (RED) covering D-06 tests 3, 4, 6.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/application/test_signal_director.py -k "fallback or checkpoint or minority or audit" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Analyst Exception → TradeSignal still returned, confidence lowered (D-06 test 4 green)
    - confidence < 0.65 → exactly one warning/checkpoint call + disclaimer prefix (D-06 test 6 green)
    - agent_run passed to insert contains bear_case in 1-Bear-vs-3-Bull scenario (D-06 test 3 green)
    - repo.insert called once; UUID present in TradeSignal
  </acceptance_criteria>
  <done>Fallback, HITL checkpoint, and audit-trail persistence all tested green.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/application/test_signal_director.py -q` green (D-06 tests 3, 4, 6 covered)
</verification>

<success_criteria>
- SignalDirector returns a resilient, audited TradeSignal; minority protection + fallback + checkpoint enforced
</success_criteria>

<output>
Create `.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-05-SUMMARY.md` when done
</output>
