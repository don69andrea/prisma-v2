---
phase: 1
plan: 02
type: feature
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0038_crypto_onchain_history.py
  - backend/alembic/versions/0039_market_sentiment.py
  - backend/infrastructure/adapters/coin_metrics_adapter.py
  - backend/infrastructure/adapters/fear_greed_adapter.py
  - backend/tests/unit/infrastructure/test_coin_metrics_adapter.py
  - backend/tests/unit/infrastructure/test_fear_greed_adapter.py
autonomous: true
requirements:
  - "crypto_onchain_history table with Coin Metrics data"
  - "market_sentiment table with Fear&Greed history"
  - "Graceful fallback when coin not available in Coin Metrics"
---

<objective>
Add on-chain data (Coin Metrics Community) and Fear&Greed sentiment (alternative.me) adapters plus their DB migrations. Runs in parallel with 01-01-PLAN.md (Wave 1b+1c). These are auxiliary signal features for Layer 1 and Layer 2.
</objective>

<context>
@.planning/phases/01-v4-1-signal-engine/01-CONTEXT.md
@.planning/phases/01-v4-1-signal-engine/01-RESEARCH.md
@docs/AGENTS.md
@CLAUDE.md
@backend/alembic/versions/0021_add_ml_feature_columns.py
@backend/infrastructure/adapters/yfinance_swiss.py
</context>

<tasks>
<task id="T01">
  <name>Migrations 0038+0039 + CoinMetricsAdapter</name>
  <files>
    - backend/alembic/versions/0038_crypto_onchain_history.py
    - backend/infrastructure/adapters/coin_metrics_adapter.py
    - backend/tests/unit/infrastructure/test_coin_metrics_adapter.py
  </files>
  <action>
  TEST-FIRST. Write test mocking httpx.AsyncClient.get: assert correct URL construction, assert field mapping (SplyMVRVCur→mvrv_z, RealizedCap→realized_cap, AdrActCnt→active_addresses, FlowOutExNtv-FlowInExNtv→exchange_netflow), assert None returned (not error) when coin has no data.

  Migration 0038: create table crypto_onchain_history with columns coin_id (Integer FK crypto_universe.coin_id), date (Date), mvrv_z (Float nullable), realized_cap (Float nullable), active_addresses (Float nullable), tx_volume (Float nullable), exchange_netflow (Float nullable), source (String default "coin_metrics"). PK is (coin_id, date). In downgrade(): drop_table.

  Implement CoinMetricsAdapter with async method fetch_onchain(assets: list[str], start: str = "2017-01-01") -> pd.DataFrame. Use httpx.AsyncClient (already in deps) to call https://community-api.coinmetrics.io/v4/timeseries/asset-metrics with params assets=",".join(assets), metrics="RealizedCap,SplyMVRVCur,AdrActCnt,FlowOutExNtv,FlowInExNtv", frequency="1d", start_time=start. Map response JSON "data" array to DataFrame. For missing coins: return rows with NULL values, do not raise. Retry: _RETRIES=2, _BASE_DELAY=1.0. Note: revision chain 0038 revises 0037.
  Commit: feat(data): migration 0038 crypto_onchain + coin metrics adapter
  </action>
  <verify>pytest backend/tests/unit/infrastructure/test_coin_metrics_adapter.py -v</verify>
  <acceptance_criteria>Adapter correctly maps Coin Metrics fields; NULL fallback for missing coins; migration applies; unit tests green with mocked HTTP</acceptance_criteria>
</task>

<task id="T02">
  <name>Migration 0039 + FearGreedAdapter</name>
  <files>
    - backend/alembic/versions/0039_market_sentiment.py
    - backend/infrastructure/adapters/fear_greed_adapter.py
    - backend/tests/unit/infrastructure/test_fear_greed_adapter.py
  </files>
  <action>
  TEST-FIRST. Write test mocking httpx response: assert date parsing from Unix timestamp, assert value int and classification str extraction, assert full history returned when limit=0.

  Migration 0039: create table market_sentiment with columns date (Date PK), fear_greed (Integer), fg_classification (String), source (String default "alternative_me"). In downgrade(): drop_table. Note: revision 0039 revises 0038.

  Implement FearGreedAdapter with async method fetch_history() -> pd.DataFrame. Call https://api.alternative.me/fng/?limit=0&format=json via httpx.AsyncClient. Parse JSON response["data"]: convert timestamp (str Unix) to date via datetime.fromtimestamp(int(ts), tz=UTC).date(), map value to int, value_classification to str. Return DataFrame with columns [date, fear_greed, fg_classification]. Retry: _RETRIES=2, _BASE_DELAY=1.0.
  Commit: feat(data): migration 0039 market_sentiment + fear&greed adapter
  </action>
  <verify>pytest backend/tests/unit/infrastructure/test_fear_greed_adapter.py -v</verify>
  <acceptance_criteria>Adapter parses Unix timestamps correctly; returns full history; migration applies; unit tests green with mocked HTTP</acceptance_criteria>
</task>
</tasks>

<verification>
- pytest backend/tests/unit/infrastructure/test_coin_metrics_adapter.py backend/tests/unit/infrastructure/test_fear_greed_adapter.py -v
- ruff check backend/infrastructure/adapters/coin_metrics_adapter.py backend/infrastructure/adapters/fear_greed_adapter.py
- mypy backend/infrastructure/adapters/coin_metrics_adapter.py backend/infrastructure/adapters/fear_greed_adapter.py
</verification>

<success_criteria>
- Both migrations apply in sequence (0038→0039)
- CoinMetricsAdapter handles missing coins gracefully with NULL
- FearGreedAdapter parses Unix timestamps to UTC dates
- All unit tests green (no live HTTP calls)
</success_criteria>
