---
phase: 1
plan: 05
type: feature
wave: 3
depends_on: [01-03-PLAN.md, 01-04-PLAN.md]
files_modified:
  - backend/application/signals/sizing.py
  - backend/application/signals/signal_service.py
  - backend/interfaces/rest/schemas/signals.py
  - backend/tests/unit/application/test_sizing.py
  - backend/tests/unit/application/test_signal_service.py
autonomous: true
requirements:
  - "vol_target_size: higher vol → smaller size, bounds [0, cap]"
  - "SELL action → size_factor = 0.0 (never negative)"
  - "signal_service.evaluate() returns valid SignalVector"
  - "No LLM, no external calls in signal_service"
---

<objective>
Implement Layer 3 sizing (vol-targeting + drawdown brake) and the SignalVector orchestration service that combines Layers 1-3 into a single Pydantic output per coin.
</objective>

<context>
@.planning/phases/01-v4-1-signal-engine/01-CONTEXT.md
@.planning/phases/01-v4-1-signal-engine/01-RESEARCH.md
@docs/AGENTS.md
@CLAUDE.md
@backend/interfaces/rest/schemas/backtest.py
</context>

<tasks>
<task id="T01">
  <name>sizing.py: Vol-targeting + drawdown brake</name>
  <files>
    - backend/application/signals/sizing.py
    - backend/tests/unit/application/test_sizing.py
  </files>
  <action>
  TEST-FIRST. Write tests first: test_vol_target_size_monotone (higher pred_vol → smaller size_factor, at least 5 values), test_vol_target_size_bounds (size_factor always ∈ [0.0, cap] for pred_vol in [0.05, 2.0]), test_sell_action_zero_size (any SELL action returns 0.0 regardless of pred_vol), test_drawdown_brake_halves_exposure (current_dd=-0.25, threshold=-0.20 → size halved), test_drawdown_brake_no_effect_above_threshold (current_dd=-0.10 → size unchanged).

  Implement backend/application/signals/sizing.py:
  vol_target_size(pred_vol: float, target_vol: float = 0.60, cap: float = 1.5) -> float: return float(min(max(target_vol / max(pred_vol, 1e-8), 0.0), cap)). If pred_vol ≤ 0, return cap (safety).
  drawdown_brake(size: float, current_dd: float, threshold: float = -0.20) -> float: if current_dd < threshold: return size * 0.5. Else return size.
  apply_sizing(action: str, pred_vol: float, current_dd: float = 0.0, target_vol: float = 0.60, cap: float = 1.5) -> float: if action == "SELL": return 0.0 (NEVER negative, NEVER short). Else: size = vol_target_size(pred_vol, target_vol, cap); return drawdown_brake(size, current_dd).
  Commit: feat(signals): vol-targeting sizing + drawdown brake (A7.5, A7.8)
  </action>
  <verify>pytest backend/tests/unit/application/test_sizing.py -v — monotonicity, bounds, SELL=0 all pass</verify>
  <acceptance_criteria>Monotone: higher pred_vol → smaller size; size ∈ [0, cap]; SELL → 0.0 always; drawdown brake halves when threshold breached; A7.5 and A7.8 pass</acceptance_criteria>
</task>

<task id="T02">
  <name>SignalVector schema + signal_service.evaluate()</name>
  <files>
    - backend/interfaces/rest/schemas/signals.py
    - backend/application/signals/signal_service.py
    - backend/tests/unit/application/test_signal_service.py
  </files>
  <action>
  First define Pydantic schemas in backend/interfaces/rest/schemas/signals.py:
  SignalVector(BaseModel): coin: str, asof: date, action: Literal["BUY","HOLD","SELL"], size_factor: float = Field(ge=0.0, le=1.5), consensus: str (e.g. "2/3"), sub_scores: dict[str, float] (keys: ma_signal, macd_signal, rsi_signal, vol_pred, momentum_rank, onchain_score), confidence: float = Field(ge=0.0, le=1.0), disclaimer: str = "Entscheidungsunterstützung, kein Anlagerat."
  BacktestReport(BaseModel): coin: str, cagr: float, sharpe: float, max_dd: float, calmar: float, beats_exposure_matched: bool, n_trades: int, equity_curve: list[tuple[date, float]].

  TEST-FIRST for signal_service. Write test: create mock DataFrames for prices, onchain, sentiment. Call evaluate() with mocks injected. Assert result is SignalVector instance. Assert action in ["BUY","HOLD","SELL"]. Assert size_factor == 0.0 when action == "SELL". Assert sub_scores contains expected keys. Assert disclaimer set.

  Implement backend/application/signals/signal_service.py:
  async def evaluate(coin: str, asof: date, prices_df: pd.DataFrame, onchain_df: pd.DataFrame | None = None, vol_model_info: dict | None = None) -> SignalVector. No I/O inside evaluate() — all data passed in (testable, no DB calls). Layer 1: cross_sectional_momentum(prices_df) → momentum_rank; onchain_health_score(onchain_df) → onchain_score (0.5 if None). Layer 2: compute sma(close, 100), rsi(close, 14), macd(close) for the specific coin. Generate binary signals: ma_signal=1 if close[-1]>sma[-1], rsi_signal=1 if rsi[-1]>50, macd_signal=1 if macd_hist[-1]>0. consensus = consensus_vote(signals_df). action = "BUY" if consensus==1 else "SELL" if momentum_rank > 8 else "HOLD". Layer 3: pred_vol = predict_vol(...) if vol_model_info else 0.60. size_factor = apply_sizing(action, pred_vol). Build SignalVector and return.
  Commit: feat(signals): SignalVector schema + signal_service.evaluate() orchestrating layers 1-3
  </action>
  <verify>pytest backend/tests/unit/application/test_signal_service.py -v — SignalVector returned, SELL→size=0, all keys present</verify>
  <acceptance_criteria>evaluate() returns valid SignalVector; no I/O inside evaluate (all injected); SELL always has size_factor=0.0; sub_scores has all required keys; Pydantic validation passes</acceptance_criteria>
</task>
</tasks>

<verification>
- pytest backend/tests/unit/application/test_sizing.py backend/tests/unit/application/test_signal_service.py -v
- python -c "from backend.interfaces.rest.schemas.signals import SignalVector, BacktestReport; print('schemas OK')"
- ruff check backend/application/signals/sizing.py backend/application/signals/signal_service.py backend/interfaces/rest/schemas/signals.py
- mypy backend/application/signals/sizing.py backend/application/signals/signal_service.py
</verification>

<success_criteria>
- Sizing monotone, bounded, SELL=0.0 (A7.5, A7.8)
- SignalVector Pydantic model validates correctly
- signal_service.evaluate() testable without DB (injected data)
- Action logic correct: BUY=consensus, SELL if poor rank, HOLD otherwise
- ruff + mypy clean
</success_criteria>
