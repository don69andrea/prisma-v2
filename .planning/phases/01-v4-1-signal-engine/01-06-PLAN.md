---
phase: 1
plan: 06
type: feature
wave: 4
depends_on: [01-05-PLAN.md]
files_modified:
  - backend/application/backtest/__init__.py
  - backend/application/backtest/guards.py
  - backend/application/backtest/walkforward.py
  - backend/tests/unit/application/test_guards.py
  - backend/tests/unit/application/test_walkforward.py
autonomous: true
requirements:
  - "Look-ahead guard raises ValueError when feature uses data@t"
  - "Exposure-matched baseline computed alongside strategy"
  - "Net costs 0.1% per trade deducted"
  - "beats_exposure_matched: strategy Sharpe AND Calmar > baseline"
  - "Reproduce PoC: strategy Calmar > baseline Calmar"
---

<objective>
Build the strict backtest engine: Look-Ahead-Guard (A7.2) and expanding-window walk-forward with exposure-matched baseline and net costs (A7.6, A7.7). This is the honest evidence engine — it must reproduce the PoC finding.
</objective>

<context>
@.planning/phases/01-v4-1-signal-engine/01-CONTEXT.md
@.planning/phases/01-v4-1-signal-engine/01-RESEARCH.md
@docs/AGENTS.md
@CLAUDE.md
@docs/research/poc_feasibility.py
@docs/research/poc_results.txt
@docs/research/indicator_results.txt
</context>

<tasks>
<task id="T01">
  <name>guards.py: Look-Ahead-Guard (A7.2)</name>
  <files>
    - backend/application/backtest/__init__.py
    - backend/application/backtest/guards.py
    - backend/tests/unit/application/test_guards.py
  </files>
  <action>
  TEST-FIRST. Write tests: test_guard_passes_on_shifted_feature (feature = close.shift(1) → no error raised), test_guard_raises_on_unshifted_feature (feature = close directly → ValueError raised), test_guard_checks_multiple_columns (3 columns, 1 unshifted → ValueError names the bad column), test_guard_ignores_first_row_nan (shift creates NaN in row 0 — this is expected, not a look-ahead).

  Implement backend/application/backtest/guards.py:
  assert_no_lookahead(df: pd.DataFrame, feature_cols: list[str], price_col: str = "close") -> None. For each feature_col: compute correlation of df[feature_col] with df[price_col] at lag 0 vs lag 1. If corr(feature, price.shift(0)) > corr(feature, price.shift(1)) + 0.05 (i.e. feature correlates more with CURRENT price than lagged) → raise ValueError(f"Look-ahead detected in column {feature_col}"). Alternative simpler check: if df[feature_col].equals(df[price_col]) or df[feature_col].corr(df[price_col]) > 0.999 → raise ValueError. Use the simpler check for determinism.

  Create backend/application/backtest/__init__.py (empty).
  Commit: feat(backtest): look-ahead guard (A7.2)
  </action>
  <verify>pytest backend/tests/unit/application/test_guards.py -v — shifted features pass, unshifted raise ValueError</verify>
  <acceptance_criteria>Guard correctly identifies unshifted features; ValueError names the bad column; shifted features pass silently; A7.2 satisfied</acceptance_criteria>
</task>

<task id="T02">
  <name>walkforward.py: Expanding-window backtest + exposure-matched baseline</name>
  <files>
    - backend/application/backtest/walkforward.py
    - backend/tests/unit/application/test_walkforward.py
  </files>
  <action>
  TEST-FIRST. Write tests: test_backtest_deducts_costs (strategy return < gross when there are trades — A7.7), test_exposure_matched_baseline_computed (BacktestReport.beats_exposure_matched is a bool, not None — A7.6), test_strategy_beats_baseline_on_poc_data (load docs/research/BTC-USD.csv, run walkforward with 100-day MA signal, assert beats_exposure_matched=True — this reproduces the PoC), test_no_shorting_in_equity (equity curve never has negative position from a SELL signal).

  Implement backend/application/backtest/walkforward.py:
  run_walkforward(prices: pd.DataFrame, signals: pd.Series, costs: float = 0.001, min_train: int = 252, step: int = 63) -> BacktestReport. prices has close column, signals is Series[0/1] (1=invested, 0=cash) aligned to prices.index. Strategy: daily_ret = prices["close"].pct_change() * signals.shift(1). Deduct costs: cost_series = (signals.diff().abs() * costs).fillna(0); net_ret = daily_ret - cost_series. Exposure fraction: avg_exposure = signals.mean(). Baseline (exposure-matched): baseline_ret = prices["close"].pct_change() * avg_exposure (continuous, no timing). Compute metrics for both: CAGR, Sharpe (annualized ×sqrt(252)), MaxDD, Calmar. beats_exposure_matched = strategy.sharpe > baseline.sharpe AND strategy.calmar > baseline.calmar. equity_curve = [(date, equity_value), ...] as list of tuples. Return BacktestReport from backend.interfaces.rest.schemas.signals.
  Commit: feat(backtest): walk-forward engine + exposure-matched baseline + net costs (A7.6, A7.7)
  </action>
  <verify>pytest backend/tests/unit/application/test_walkforward.py -v — costs deducted, baseline computed, PoC reproduction test passes (BTC beats_exposure_matched=True)</verify>
  <acceptance_criteria>Net costs deducted (A7.7); exposure-matched baseline always computed (A7.6); beats_exposure_matched=True on PoC BTC data; BacktestReport Pydantic-valid; equity curve non-negative</acceptance_criteria>
</task>
</tasks>

<verification>
- pytest backend/tests/unit/application/test_guards.py backend/tests/unit/application/test_walkforward.py -v
- python -c "import pandas as pd; df=pd.read_csv('docs/research/BTC-USD.csv', index_col=0, parse_dates=True); print('PoC data loaded:', len(df))"
- ruff check backend/application/backtest/
- mypy backend/application/backtest/guards.py backend/application/backtest/walkforward.py
</verification>

<success_criteria>
- Look-ahead guard correctly blocks unshifted features (A7.2)
- Net costs deducted from strategy returns (A7.7)
- Exposure-matched baseline always computed (A7.6)
- PoC reproduction: BTC beats_exposure_matched=True (Strategy Calmar > Baseline Calmar)
- No negative equity from SELL signals
- ruff + mypy clean
</success_criteria>
