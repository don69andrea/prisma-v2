---
phase: 02-v4-2-meta-labeling-planned
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - backend/tests/unit/application/test_meta_label.py
  - backend/application/signals/meta_label.py
autonomous: true
requirements: [ML-05, ML-06]
must_haves:
  truths:
    - "fit_meta_classifier(X, y, model='logreg') fits a LogisticRegression with L2, max_iter=1000, class_weight='balanced' and returns a model_info dict"
    - "predict_meta_label runs an expanding-window walk-forward (min_train=252, step=21, embargo=5) and reports per-fold precision/recall/F1"
    - "OOS precision exceeds 50% on a synthetic dataset with a learnable pattern (ML-05)"
    - "the classifier is never fitted on OOS folds — train slice always ends before test_start = train_end + embargo (ML-06)"
  artifacts:
    - path: "backend/application/signals/meta_label.py"
      provides: "fit_meta_classifier, predict_meta_label, _walkforward_meta_cv"
      contains: "def fit_meta_classifier"
      min_lines: 80
    - path: "backend/tests/unit/application/test_meta_label.py"
      provides: "Wave B classifier walk-forward tests (ML-05, ML-06)"
      contains: "test_classifier_oos_above_random"
  key_links:
    - from: "backend/application/signals/meta_label.py (predict_meta_label)"
      to: "sklearn.linear_model.LogisticRegression"
      via: "lazy import inside fit_meta_classifier; LightGBM fallback per D Classifier Algorithm"
      pattern: "LogisticRegression"
    - from: "backend/application/signals/meta_label.py (_walkforward_meta_cv)"
      to: "backend/application/signals/vol_forecast.py (_fit_single_coin)"
      via: "expanding-window loop pattern copied, plus 5-day embargo gap"
      pattern: "embargo"
---

<objective>
Wave B — Classifier + Walk-Forward. Add the binary meta-classifier and its strict expanding-window walk-forward loop on top of the Wave A labels/features. Test-First: walk-forward and no-snooping tests are RED before implementation.

Purpose: This is the "take trade now / skip" decision engine. Per D (Classifier Algorithm) LogisticRegression is primary (interpretable coefficients for the explainability requirement) with a LightGBM fallback behind the same `fit_meta_classifier(X, y, model=...)` interface. Per D (Walk-Forward Structure) the loop uses min_train=252, step=21, embargo=5, and requires >=10 OOS folds for a valid result.
Output: `fit_meta_classifier`, `predict_meta_label`, `_walkforward_meta_cv` in meta_label.py; ML-05 and ML-06 green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-CONTEXT.md
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-RESEARCH.md
@backend/application/signals/vol_forecast.py
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — write failing classifier walk-forward tests (ML-05, ML-06)</name>
  <files>backend/tests/unit/application/test_meta_label.py</files>
  <read_first>
    - backend/tests/unit/application/test_meta_label.py (existing Wave A tests + _import helper to extend)
    - backend/application/signals/vol_forecast.py (_fit_single_coin walk-forward loop being adapted)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-CONTEXT.md (Test Strategy items 5, 6; Walk-Forward Structure)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-RESEARCH.md (Pattern 3; Pitfall 2 embargo; Pitfall 3 class imbalance; Pitfall 6 min folds)
  </read_first>
  <behavior>
    - test_classifier_oos_above_random (ML-05): build a synthetic feature matrix where the label is a deterministic function of one feature (learnable). The walk-forward mean OOS precision must be > 0.50.
    - test_no_snooping (ML-06): assert that for every fold the train slice index max is strictly less than the test slice index min, with a gap of at least `embargo` rows (train never overlaps OOS, and the embargo dead-zone is honored).
  </behavior>
  <action>
    Extend the existing test file. Add to `_import()` (or a second lazy import helper) the symbols `fit_meta_classifier`, `predict_meta_label`, `_walkforward_meta_cv`. Add a fixture `_learnable_xy(n=600)` producing X (DataFrame of >=2 numeric features, DatetimeIndex) and y (binary Series) where y is a clean function of one feature plus light noise, large enough for >=10 folds (n >= min_train + embargo + 10*step ≈ 252 + 5 + 210). For ML-05 call the walk-forward entry point and assert mean OOS precision > 0.50. For ML-06, exercise `_walkforward_meta_cv` and verify the fold-boundary invariant: for each fold, `train_end_index + embargo <= test_start_index` (no snooping, embargo honored). Keep <=100 chars/line. Run → RED (the new symbols do not exist yet). Commit `test(meta-label): add RED classifier walk-forward tests (ML-05, ML-06)`.
  </action>
  <verify>
    <automated>cd /Users/andreapetretta/prisma-v2 && pytest backend/tests/unit/application/test_meta_label.py -q -k "oos_above_random or no_snooping" 2>&1 | grep -qiE "error|failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Tests test_classifier_oos_above_random and test_no_snooping exist (grep finds both names).
    - Suite RED on the two new tests (symbols not yet implemented).
    - Wave A tests still green (no regression introduced by the edits).
  </acceptance_criteria>
  <done>Two new RED tests exist; existing Wave A tests remain green.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement fit_meta_classifier, predict_meta_label, _walkforward_meta_cv</name>
  <files>backend/application/signals/meta_label.py</files>
  <read_first>
    - backend/tests/unit/application/test_meta_label.py (ML-05, ML-06 expectations)
    - backend/application/signals/vol_forecast.py (_fit_single_coin loop + lazy LightGBM import to mirror)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-RESEARCH.md (Pattern 3 exact loop; Pitfall 2/3/6; Binary Label Mapping example)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-CONTEXT.md (Classifier Algorithm; Walk-Forward Structure)
  </read_first>
  <action>
    Add three functions to meta_label.py:

    fit_meta_classifier(X, y, model='logreg') -> dict: lazy-import sklearn. For 'logreg' build LogisticRegression(C=1.0, max_iter=1000, class_weight='balanced', random_state=42). For 'lgbm' build LGBMClassifier(n_estimators=100, max_depth=4, learning_rate=0.05, n_jobs=1, verbose=-1) — same config as vol_forecast. Fit on X.values, y.values; return model_info dict {model, model_type, feature_cols}. Set class_weight='balanced' by default to defend the crypto class-imbalance pitfall (Pitfall 3).

    _walkforward_meta_cv(X, y, min_train=252, step=21, embargo=5, model_type='logreg') -> dict: concat X and y, dropna; expanding loop `for start in range(min_train, n - step - embargo + 1, step)`: train = data.iloc[:start]; test_start = start + embargo; test = data.iloc[test_start : test_start + step]; skip folds where train has <2 classes; fit via fit_meta_classifier; predict; collect precision/recall/f1 (sklearn.metrics, zero_division=0), n_trades_taken, n_trades_skipped. Return {"folds": [...], "n_folds": len}. The embargo gap is the no-snooping guarantee (Pitfall 2).

    predict_meta_label(X, y, min_train=252, step=21, embargo=5, model='logreg') -> dict: run _walkforward_meta_cv, aggregate mean precision/recall, and refit a final model on all rows for production scoring. If n_folds < 10, mark the result so the caller can emit finding="negative"/"insufficient_oos_folds" (Pitfall 6) — do not fabricate metrics.

    Map labels to binary at the boundary the caller controls (Binary Label Mapping: +1→1, else 0); keep _walkforward_meta_cv label-agnostic (expects already-binary y). mypy strict annotations; lazy imports; <=100 chars/line. Run → GREEN. Commit `feat(meta-label): add meta-classifier walk-forward (logreg + lgbm fallback)`.
  </action>
  <verify>
    <automated>cd /Users/andreapetretta/prisma-v2 && pytest backend/tests/unit/application/test_meta_label.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from backend.application.signals.meta_label import fit_meta_classifier, predict_meta_label"` exits 0.
    - ML-05 (mean OOS precision > 0.50) and ML-06 (embargo/no-snooping invariant) pass.
    - _walkforward_meta_cv uses test_start = start + embargo (grep finds the embargo offset in the slice).
    - LogisticRegression configured with max_iter=1000 and class_weight='balanced'.
    - `ruff check backend/application/signals/meta_label.py && mypy backend/application/signals/meta_label.py` pass.
  </acceptance_criteria>
  <done>ML-05, ML-06 green; classifier + walk-forward exported; all Wave A+B tests pass; ruff + mypy clean.</done>
</task>

</tasks>

<artifacts_produced>
New symbols introduced this plan:
- `fit_meta_classifier(X, y, model='logreg') -> dict` in backend/application/signals/meta_label.py
- `_walkforward_meta_cv(X, y, min_train=252, step=21, embargo=5, model_type='logreg') -> dict` in backend/application/signals/meta_label.py
- `predict_meta_label(X, y, min_train=252, step=21, embargo=5, model='logreg') -> dict` in backend/application/signals/meta_label.py
- Tests: test_classifier_oos_above_random, test_no_snooping
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| feature matrix → classifier fit | In-process numeric input; no untrusted input at this layer |
| train fold → test fold | Temporal boundary: OOS data must never reach the fit step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information disclosure | label leakage from train horizon into test (no embargo) | mitigate | 5-day embargo gap in _walkforward_meta_cv (test_start=start+embargo); ML-06 invariant test blocks merge |
| T-02-04 | Repudiation | unreliable metrics from too-few folds | mitigate | n_folds >= 10 check; emit finding="negative"/"insufficient_oos_folds" instead of fabricated numbers (Pitfall 6) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages — scikit-learn/lightgbm already in pyproject.toml (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest backend/tests/unit/application/test_meta_label.py -q` fully green (Wave A + Wave B).
- `ruff check backend/ && ruff format --check backend/ && mypy backend/application/signals/meta_label.py` all pass.
</verification>

<success_criteria>
LogisticRegression-primary classifier with LightGBM fallback behind one interface; expanding-window walk-forward with 5-day embargo; OOS precision > 50% on learnable synthetic data; no-snooping invariant proven; TDD order preserved.
</success_criteria>

<output>
Create `.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-02-SUMMARY.md` when done
</output>
