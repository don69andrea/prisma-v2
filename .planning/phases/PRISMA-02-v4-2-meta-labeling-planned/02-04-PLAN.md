---
phase: 02-v4-2-meta-labeling-planned
plan: 04
type: tdd
wave: 4
depends_on: [02-03]
files_modified:
  - backend/tests/unit/application/test_meta_label.py
  - backend/interfaces/rest/routers/signals.py
autonomous: true
requirements: [ML-09, ML-10]
must_haves:
  truths:
    - "GET /api/v1/signals/meta-label/{coin} returns a valid MetaLabelReport Pydantic object (no freetext)"
    - "unknown coin returns 404 with the _CRYPTO_UNIVERSE whitelist message (input validation)"
    - "the endpoint runs the synchronous meta-label pipeline via asyncio.to_thread (not run_in_executor)"
    - "full backend unit suite passes the coverage gate of >= 80%"
  artifacts:
    - path: "backend/interfaces/rest/routers/signals.py"
      provides: "GET /api/v1/signals/meta-label/{coin} endpoint + async wrapper"
      contains: "meta-label/{coin}"
    - path: "backend/tests/unit/application/test_meta_label.py"
      provides: "REST endpoint test returning MetaLabelReport (ML-09)"
      contains: "test_rest_returns_pydantic"
  key_links:
    - from: "backend/interfaces/rest/routers/signals.py (meta-label endpoint)"
      to: "backend/application/signals/meta_label.py (predict_meta_label)"
      via: "async wrapper calls the sync pipeline via asyncio.to_thread"
      pattern: "asyncio.to_thread"
    - from: "backend/interfaces/rest/routers/signals.py (meta-label endpoint)"
      to: "backend/interfaces/rest/schemas/signals.py (MetaLabelReport)"
      via: "response_model=MetaLabelReport"
      pattern: "response_model=MetaLabelReport"
---

<objective>
Wave D — API + Coverage Gate. Expose the meta-labeling pipeline through a read-only REST endpoint returning the MetaLabelReport contract, and bring the full backend unit suite over the >= 80% coverage gate. Test-First: the endpoint test is RED before the route exists.

Purpose: This is the thin interface layer (D Module Structure: new endpoint GET /api/v1/signals/meta-label/{coin}). It must follow the existing signals router conventions — coin whitelist validation against _CRYPTO_UNIVERSE (V5 input validation), stub prices (n=500) like get_backtest, and asyncio.to_thread for the sync compute (CLAUDE.md async rule, NOT run_in_executor). Coverage >= 80% is a non-negotiable CI gate (D Success Criterion / Test Strategy item 8).
Output: working endpoint, ML-09 green, full suite green at coverage >= 80%, ready for PR on feat/v4-2-meta-labeling.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-CONTEXT.md
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-RESEARCH.md
@backend/interfaces/rest/routers/signals.py
@backend/interfaces/rest/schemas/signals.py
@.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-03-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — endpoint returns MetaLabelReport + 404 on unknown coin (ML-09)</name>
  <files>backend/tests/unit/application/test_meta_label.py</files>
  <read_first>
    - backend/interfaces/rest/routers/signals.py (get_backtest pattern: _CRYPTO_UNIVERSE check, _make_stub_prices, async wrapper, 404 shape)
    - backend/interfaces/rest/schemas/signals.py (MetaLabelReport fields to assert against)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-CONTEXT.md (Test Strategy item 9)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-RESEARCH.md (Security Domain: coin whitelist V5, DoS via stub prices)
  </read_first>
  <behavior>
    - test_rest_returns_pydantic (ML-09): GET /api/v1/signals/meta-label/BTC-USD returns 200 and a body that validates as MetaLabelReport (all required fields present, finding in the allowed Literal set).
    - test_meta_label_unknown_coin_404: GET /api/v1/signals/meta-label/FAKE-USD returns 404 (coin not in _CRYPTO_UNIVERSE).
  </behavior>
  <action>
    Add a FastAPI TestClient (or httpx ASGITransport) based test against the app router. Mount the signals router in a minimal FastAPI app fixture if no app-level fixture exists. For ML-09 call the meta-label route and validate the JSON body with `MetaLabelReport.model_validate(resp.json())`; assert `resp.json()["finding"]` is one of {"positive","secondary_pass","negative"}. For the 404 test, request an unknown coin and assert status 404. Keep <=100 chars/line. Run → RED (route 404s for the valid coin too, because it does not exist yet). Commit `test(meta-label): add RED REST endpoint tests (ML-09)`.
  </action>
  <verify>
    <automated>cd /Users/andreapetretta/prisma-v2 && pytest backend/tests/unit/application/test_meta_label.py -q -k "rest_returns_pydantic or unknown_coin_404" 2>&1 | grep -qiE "error|failed" && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - Tests test_rest_returns_pydantic and test_meta_label_unknown_coin_404 present.
    - Suite RED for the valid-coin route (endpoint not yet registered).
  </acceptance_criteria>
  <done>Two RED endpoint tests target the not-yet-existing meta-label route.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement meta-label endpoint + async wrapper</name>
  <files>backend/interfaces/rest/routers/signals.py</files>
  <read_first>
    - backend/tests/unit/application/test_meta_label.py (ML-09 + 404 expectations)
    - backend/interfaces/rest/routers/signals.py (run_walkforward async wrapper + get_backtest to mirror exactly)
    - backend/application/signals/meta_label.py (predict_meta_label / build_meta_features signatures from Waves A+B)
    - backend/application/backtest/walkforward.py (run_walkforward_with_details meta_filter for always-trade vs filtered comparison)
  </read_first>
  <action>
    Add `GET /{coin}` under the existing `router` (prefix /api/v1/signals) at path "/meta-label/{coin}" with response_model=MetaLabelReport. Validate coin against _CRYPTO_UNIVERSE (uppercase, 404 with the existing whitelist message — same shape as get_signal/get_backtest, V5 input validation). Build stub prices via _make_stub_prices(coin_upper, n=500) (same DoS-bounded source as get_backtest). Add a module-level async wrapper `async def run_meta_label(coin, prices_df) -> MetaLabelReport` that does the sync compute inside `asyncio.to_thread(_sync_call)` (NOT run_in_executor — CLAUDE.md). Inside _sync_call: derive a close/high/low frame, build features via build_meta_features, build triple_barrier labels, map to binary, run predict_meta_label to get oos_precision/recall and n_folds, run run_walkforward_with_details twice (always-trade meta_filter=None vs meta-filtered meta_filter=predicted labels) on the same dates to fill always_trade_/meta_filtered_ Sharpe+Calmar and n_trades, then compute beats_baseline and finding per the success criterion (positive if both Sharpe AND Calmar improve; secondary_pass if trade count or max_dd reduced >=10% without >5% Sharpe/Calmar loss; negative otherwise or if n_folds<10 with reason). Return MetaLabelReport. Import MetaLabelReport and the meta_label functions at module top. mypy strict; <=100 chars/line. Run → GREEN. Commit `feat(meta-label): add GET /api/v1/signals/meta-label/{coin} endpoint`.
  </action>
  <verify>
    <automated>cd /Users/andreapetretta/prisma-v2 && pytest backend/tests/unit/application/test_meta_label.py -q -k "rest_returns_pydantic or unknown_coin_404"</automated>
  </verify>
  <acceptance_criteria>
    - GET /api/v1/signals/meta-label/BTC-USD returns 200 + valid MetaLabelReport (ML-09 passes).
    - Unknown coin returns 404 with the whitelist message.
    - Endpoint uses asyncio.to_thread (grep finds asyncio.to_thread, and run_in_executor is absent from the new code).
    - `ruff check backend/ && mypy backend/interfaces/rest/routers/signals.py` pass.
  </acceptance_criteria>
  <done>Endpoint live, ML-09 green, follows whitelist + asyncio.to_thread conventions.</done>
</task>

<task type="auto">
  <name>Task 3: Coverage gate >= 80% + full-suite green (ML-10)</name>
  <files>backend/tests/unit/application/test_meta_label.py</files>
  <read_first>
    - backend/application/signals/meta_label.py (uncovered branches: NaN/zero-ATR guard, lgbm fallback path, n_folds<10 negative path)
    - .planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-RESEARCH.md (Validation Architecture: full suite command, coverage gate)
  </read_first>
  <action>
    Run the full coverage command and read the per-file coverage for the new modules. Add the minimum focused tests to clear >= 80% on meta_label.py and the new endpoint code: cover the triple_barrier NaN/zero-ATR → 0 branch, the trend_scan no-window-exceeds-threshold → 0 branch, the lgbm fallback path of fit_meta_classifier (model='lgbm'), and the n_folds<10 → finding="negative"/"insufficient_oos_folds" branch. Do NOT lower the fail_under threshold and do NOT add `# pragma: no cover` to dodge real branches. Keep <=100 chars/line. Commit `test(meta-label): cover edge branches to clear 80% gate`.
  </action>
  <verify>
    <automated>cd /Users/andreapetretta/prisma-v2 && pytest backend/tests/unit -q --cov=backend --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - `pytest backend/tests/unit -q --cov=backend --cov-fail-under=80` exits 0 (gate met).
    - All ML-01..ML-10 tests green.
    - `ruff check backend/ && ruff format --check backend/ && mypy backend/` all pass.
    - No reduction of fail_under in pyproject.toml and no new blanket `# pragma: no cover`.
  </acceptance_criteria>
  <done>Full unit suite green at coverage >= 80%; ruff + mypy clean; phase ready for PR on feat/v4-2-meta-labeling.</done>
</task>

</tasks>

<artifacts_produced>
New symbols introduced this plan:
- `GET /api/v1/signals/meta-label/{coin}` route (function get_meta_label) in backend/interfaces/rest/routers/signals.py
- `run_meta_label(coin, prices_df) -> MetaLabelReport` async wrapper in backend/interfaces/rest/routers/signals.py
- Tests: test_rest_returns_pydantic, test_meta_label_unknown_coin_404, plus edge-branch coverage tests
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST API (coin path param) | Untrusted path segment; validated against _CRYPTO_UNIVERSE whitelist |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | coin path-param injection | mitigate | Whitelist check against _CRYPTO_UNIVERSE; 404 if absent (same pattern as get_backtest) — V5 input validation |
| T-02-08 | Denial of service | large compute via user-controlled window | mitigate | Window fixed via _make_stub_prices(n=500); no user-controlled size (RESEARCH Security Domain) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest backend/tests/unit -q --cov=backend --cov-fail-under=80` green.
- `ruff check backend/ && ruff format --check backend/ && mypy backend/` all pass.
- ML-01 through ML-10 all green.
</verification>

<success_criteria>
Read-only meta-label endpoint returns a valid MetaLabelReport with coin-whitelist validation and asyncio.to_thread compute; full backend unit suite green at coverage >= 80%; phase ready for PR to main.
</success_criteria>

<output>
Create `.planning/phases/PRISMA-02-v4-2-meta-labeling-planned/02-04-SUMMARY.md` when done
</output>
