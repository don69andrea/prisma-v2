---
phase: 04-v4-4-rag-sentiment-planned
plan: 06
type: tdd
wave: 5
depends_on: ["04-01", "04-05"]
files_modified:
  - backend/application/agents/signal_director.py
  - backend/tests/integration/test_agent_mandatory_suite.py
autonomous: true
requirements: [REQ-4-08]
must_haves:
  truths:
    - "When SENTIMENT_ENABLED=true and senti.veto, action is forced to HOLD (BUY blocked, SELL never upgraded) (D-06)"
    - "When SENTIMENT_ENABLED=true and senti.score < 0, size_factor is scaled down by (1 + score*0.3), clamped >= 0 (D-06)"
    - "Positive score does NOT amplify size (downside-only) (D-06)"
    - "When SENTIMENT_ENABLED=false, behavior is identical to the V4-3 stub — no veto override, no size change (C-04, D-06)"
    - "All 7 existing D-06 mandatory tests stay green"
  artifacts:
    - path: "backend/application/agents/signal_director.py"
      provides: "_synthesize() veto + downside-only size scaling behind sentiment_enabled"
      contains: "sentiment_enabled"
  key_links:
    - from: "backend/application/agents/signal_director.py"
      to: "backend/config.py"
      via: "get_settings().sentiment_enabled guard"
      pattern: "get_settings\\(\\)"
---

<objective>
Wire the sentiment veto and downside-only size scaling into SignalDirector._synthesize()
(TDD), both guarded by SENTIMENT_ENABLED (default false, C-04) per D-06. Extend the
mandatory test suite with the four new code-path tests while keeping the existing 7
D-06 guards green.

Purpose: This is where sentiment becomes functional — a veto that blocks BUY and a
size scaler that only ever reduces exposure (no shorting). The flag default-false means
zero behavior change until the backtest (04-07) justifies enabling it.
Output: targeted insertions in _synthesize() + 4 new mandatory-suite tests.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-RESEARCH.md
@.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md
@CLAUDE.md
@AGENTS.md
@backend/application/agents/signal_director.py
@backend/config.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: _synthesize() veto override + downside-only size scaling (D-06)</name>
  <files>backend/application/agents/signal_director.py</files>
  <read_first>
    - backend/application/agents/signal_director.py (_synthesize body 113-178; action determination line 151; no-shorting clamp lines 153-157; senti_agent.analyze(coin, {}) call line 242)
    - backend/config.py (get_settings lru_cache singleton lines 111-114 — C-03 access path)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-RESEARCH.md (Pattern 5 + Code Examples "Current _synthesize() insertion point")
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (section "signal_director.py" — exact insertion points)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-CONTEXT.md (D-06 exact code)
  </read_first>
  <behavior>
    - Test: SENTIMENT_ENABLED=true + senti.veto=True + engine action BUY → TradeSignal.action == "HOLD".
    - Test: SENTIMENT_ENABLED=true + senti.veto=True + engine action SELL → action stays SELL (veto never upgrades SELL).
    - Test: SENTIMENT_ENABLED=false + senti.veto=True → action unchanged (no override) (C-04).
    - Test: SENTIMENT_ENABLED=true + senti.score=-0.5 → size_factor == original * (1 + (-0.5)*0.3), and >= 0.
    - Test: SENTIMENT_ENABLED=true + senti.score=+0.5 → size_factor unchanged (no amplification).
    - Test: SENTIMENT_ENABLED=false + senti.score=-0.5 → size_factor unchanged.
  </behavior>
  <action>
    RED: add the new parametrized tests (Task 2 file) and confirm they fail against the current _synthesize().
    GREEN: Insert, after `action = _action_from_engine(engine_signal.action)` (line 151), the veto block guarded by the singleton: read `_settings = get_settings()` (C-03 — never instantiate Settings() directly); `if _settings.sentiment_enabled and senti.veto: action = "HOLD"` (D-06; blocks BUY, never upgrades SELL). Leave the existing no-shorting clamp (lines 153-157) untouched. After that clamp insert: `if _settings.sentiment_enabled and senti.score < 0: size_factor = size_factor * (1 + senti.score * 0.3); size_factor = max(0.0, size_factor)` (D-06 downside-only; positive score never amplifies). Import get_settings from backend.config. Both branches are no-ops when sentiment_enabled is False (C-04). Do not change the senti_agent.analyze(coin, {}) call site.
    REFACTOR: none — keep insertions minimal and surgical.
  </action>
  <verify>
    <automated>pytest backend/tests/integration/test_agent_mandatory_suite.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/application/agents/signal_director.py` contains `get_settings()` and `senti.veto` and `senti.score * 0.3`
    - no direct `Settings()` instantiation introduced (grep -v '^#' for `Settings()` shows only get_settings usage)
    - `pytest backend/tests/integration/test_agent_mandatory_suite.py -x` exits 0 (existing 7 + new 4)
  </acceptance_criteria>
  <done>_synthesize() forces HOLD on veto and scales size down on negative score only, both behind sentiment_enabled; false = identical to V4-3.</done>
</task>

<task type="execute">
  <name>Task 2: Extend mandatory suite with 4 D-06 sentiment-wiring tests</name>
  <files>backend/tests/integration/test_agent_mandatory_suite.py</files>
  <read_first>
    - backend/tests/integration/test_agent_mandatory_suite.py (header lines 1-41; _make_director factory lines 130-200; the existing 7 D-06 guards — must stay green)
    - .planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-PATTERNS.md (section "test_backtest_sentiment_comparison.py" + _make_director pattern; the 4 required assertions)
    - backend/config.py (get_settings — tests must patch/override sentiment_enabled; clear lru_cache between cases)
  </read_first>
  <action>
    Add 4 tests using the existing _make_director factory with a mocked senti_agent.analyze returning a controlled SentimentView: (1) sentiment_enabled=true + veto=True + BUY engine → action HOLD; (2) sentiment_enabled=false + veto=True → action unchanged; (3) sentiment_enabled=true + score=-0.5 → size scaled down; (4) sentiment_enabled=true + score=+0.5 → size unchanged (Pitfall 7). Toggle the flag by monkeypatching get_settings or setting the env var and calling get_settings.cache_clear() so each case reads a fresh singleton (C-03). Do NOT modify or weaken the existing 7 guards. Mark pytest.mark.integration.
  </action>
  <verify>
    <automated>pytest backend/tests/integration/test_agent_mandatory_suite.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/integration/test_agent_mandatory_suite.py` contains at least 4 new test functions referencing `sentiment_enabled`
    - the existing 7 mandatory-suite test function names are still present (grep — none deleted)
    - `pytest backend/tests/integration/test_agent_mandatory_suite.py -x` exits 0
  </acceptance_criteria>
  <done>Four new tests cover veto-on/off and size-scaling-down/no-amplify; all 11 mandatory tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SentimentView → trade action | Sentiment can override the engine action and reduce size |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Elevation of Privilege | veto/size manipulated via votes | mitigate | Veto consumes a bool from a hard-coded-threshold rule; size scaling is downside-only and clamped >= 0 (no shorting). Both gated by sentiment_enabled (default false). |
| T-4-03 | Tampering | numeric score path | mitigate | score reaching _synthesize() is already deterministic Python output (plan 04-05); no LLM number enters here. |
| T-4-SC | Tampering | npm/pip installs | accept | No new packages (RESEARCH Package Legitimacy Audit clean). |
</threat_model>

<verification>
- pytest backend/tests/integration/test_agent_mandatory_suite.py -q passes (11 tests)
- grep confirms get_settings() singleton used, no direct Settings()
- Existing 7 D-06 guards unchanged
</verification>

<success_criteria>
- Veto forces HOLD; SELL never upgraded; flag-false = no-op
- Downside-only size scaling; positive score never amplifies
- All mandatory-suite tests green
</success_criteria>

<output>
Create `.planning/phases/PRISMA-04-v4-4-rag-sentiment-planned/04-06-SUMMARY.md` when done
</output>
