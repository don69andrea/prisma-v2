---
phase: 03-v4-3-agentic-layer
plan: 06
type: tdd
wave: 6
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05"]
files_modified:
  - backend/interfaces/rest/routers/signals.py
  - backend/interfaces/rest/dependencies.py
  - backend/tests/integration/test_agent_signal_endpoint.py
  - backend/tests/integration/test_agent_mandatory_suite.py
autonomous: true
requirements: [REQ-3.9, REQ-3.10]
must_haves:
  truths:
    - "GET /api/v1/agent-signal/{coin} returns a TradeSignal; 404 unknown coin, 503 LLM unavailable"
    - "All 7 mandatory §6/D-06 tests are green as one runnable suite"
    - "Backend coverage gate ≥ 80% passes"
  artifacts:
    - path: "backend/interfaces/rest/routers/signals.py"
      provides: "agent-signal GET endpoint wired to SignalDirector via Depends"
      contains: "agent-signal"
    - path: "backend/tests/integration/test_agent_mandatory_suite.py"
      provides: "All 7 mandatory tests (D-06) as integration suite"
      contains: "hallucination"
  key_links:
    - from: "signals.py agent-signal endpoint"
      to: "SignalDirector.run"
      via: "Depends(get_signal_director) then await director.run(coin)"
      pattern: "get_signal_director"
---

<objective>
Expose the pipeline (D-07) and close the phase with the mandatory test suite + coverage gate. Add
`GET /api/v1/agent-signal/{coin}` to the signals router (404 unknown coin, 503 LLM unavailable, fallback
still works), wire a `get_signal_director()` Depends factory, and assemble all 7 §6/D-06 mandatory
tests as one runnable integration suite. Enforce coverage ≥ 80%.

Purpose: This is the phase's externally observable deliverable and its compliance gate. No merge until
all 7 mandatory tests are green and coverage holds.

Output: REST endpoint + DI factory + endpoint test + consolidated 7-test mandatory suite.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-CONTEXT.md
@backend/interfaces/rest/routers/signals.py
@backend/interfaces/rest/dependencies.py
@backend/domain/schemas/agent_schemas.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: GET /api/v1/agent-signal/{coin} endpoint + DI factory (RED→GREEN)</name>
  <read_first>
    - backend/interfaces/rest/routers/signals.py (get_signal 404 pattern, get_backtest 503 pattern, _CRYPTO_UNIVERSE)
    - backend/interfaces/rest/dependencies.py (get_session, Depends factory style)
    - backend/application/agents/signal_director.py (run signature, constructor deps)
  </read_first>
  <behavior>
    - Test: GET /api/v1/agent-signal/BTC returns 200 + a JSON body validating as TradeSignal (with mocked SignalDirector).
    - Test: GET /api/v1/agent-signal/FOO (not in _CRYPTO_UNIVERSE) returns 404.
    - Test: when SignalDirector raises an LLM-unavailable error that fallback cannot mask, endpoint returns 503; when fallback succeeds, returns 200 with lowered confidence.
  </behavior>
  <action>
    Add `async def get_agent_signal(coin: str, director: SignalDirector = Depends(get_signal_director))`
    to signals.py under `@router.get("/agent-signal/{coin}", response_model=TradeSignal)`. Uppercase
    coin, 404 if not in `_CRYPTO_UNIVERSE`, call `await director.run(coin_upper)`, map an
    LLM-unavailable failure to `HTTP_503_SERVICE_UNAVAILABLE` (matching the existing get_backtest 503
    pattern). Add `get_signal_director(session=Depends(get_session), settings=Depends(get_settings))`
    to dependencies.py constructing the director with its agents + repo + LLMClient. Tests first.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_agent_signal_endpoint.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - 200 + TradeSignal-valid body for known coin
    - 404 for unknown coin
    - 503 when LLM unavailable and fallback cannot recover
  </acceptance_criteria>
  <done>Endpoint live with 404/503 handling and DI factory.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Consolidated 7 mandatory tests + coverage ≥ 80% gate</name>
  <read_first>
    - 03-CONTEXT.md (D-06 — all 7 mandatory tests, exact assertions)
    - pyproject.toml (pytest config, coverage fail_under=80)
    - backend/tests/unit/application/test_analyst_agents.py, test_research_risk_agents.py, test_signal_director.py (existing per-agent assertions to reference/import)
  </read_first>
  <behavior>
    - Test 1 Hallucination-Guard: agent output number == engine/tool number, diff < 1e-9 (full pipeline).
    - Test 2 State-from-Tool: RiskAgent exposure from mocked Store, not LLM memory.
    - Test 3 Minority-Protection: 1 strong Bear vs 3 Bulls → audit trail contains bear_case; Risk can overrule.
    - Test 4 Fallback: LLM Exception → TradeSignal from engine, confidence lowered, disclaimer set.
    - Test 5 Pydantic-Schema: all agent outputs schema-validated, no freetext field.
    - Test 6 Checkpoint: confidence < 0.65 → exactly one HITL/log call, non-blocking.
    - Test 7 No-Shorting: action == "SELL" → size_factor 0.0 AND RiskVerdict.max_size 0.0, never negative.
  </behavior>
  <action>
    Create `backend/tests/integration/test_agent_mandatory_suite.py` containing all 7 D-06 tests as a
    single clearly-labeled, runnable suite (each test mapped to its D-06 number in the test name and a
    comment). Reuse the SignalDirector + mocked agents wiring. This suite is the merge gate. Then run
    the full backend test+coverage command and confirm coverage ≥ 80% (fail_under=80 from pyproject).
    If coverage is below 80% due to uncovered branches in the new agent files, add targeted tests for
    those branches (fallback paths, clamp branches) — do NOT lower the gate.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/integration/test_agent_mandatory_suite.py -x -q && python -m pytest --cov=backend --cov-fail-under=80 -q</automated>
  </verify>
  <acceptance_criteria>
    - All 7 mandatory tests green in test_agent_mandatory_suite.py
    - `pytest --cov=backend --cov-fail-under=80` exits 0
    - No D-06 assertion weakened or skipped
  </acceptance_criteria>
  <done>All 7 mandatory tests green and coverage gate ≥ 80% passes.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/integration/test_agent_signal_endpoint.py tests/integration/test_agent_mandatory_suite.py -q` green
- `python -m pytest --cov=backend --cov-fail-under=80 -q` exits 0
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → REST (agent-signal) | untrusted coin path param crosses here |
| LLM ↔ agent | LLM output is untrusted until Pydantic-validated |
| agent → audit_trail DB | append-only write boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | LLM numeric output | mitigate | Hallucination-guard test (D-06 #1) asserts agent number == engine number; Pydantic Literal/bounds reject freetext |
| T-03-02 | Spoofing | RiskAgent exposure source | mitigate | State-from-Tool (D-06 #2): exposure read from injected Store, never LLM memory |
| T-03-03 | Repudiation | agent decision history | mitigate | append-only agent_audit_trail (D-02); two-insert test proves immutability |
| T-03-04 | Denial of Service | LLM provider outage | accept/mitigate | Fallback (D-06 #4) returns engine TradeSignal; endpoint 503 only if fallback impossible |
| T-03-05 | Elevation of Privilege | no-shorting bypass | mitigate | No-Shorting (D-06 #7): SELL → size_factor & max_size forced 0.0, clamped non-negative |
| T-03-06 | Information Disclosure | path param injection | mitigate | coin validated against _CRYPTO_UNIVERSE allowlist → 404 otherwise |
</threat_model>

<success_criteria>
- agent-signal endpoint live; all 7 D-06 mandatory tests green; coverage ≥ 80% (phase merge gate satisfied)
</success_criteria>

<output>
Create `.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-06-SUMMARY.md` when done
</output>
