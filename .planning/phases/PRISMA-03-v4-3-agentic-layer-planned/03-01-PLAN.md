---
phase: 03-v4-3-agentic-layer
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - backend/domain/schemas/agent_schemas.py
  - backend/tests/unit/domain/test_agent_schemas.py
autonomous: true
requirements: [REQ-3.5, REQ-3.10]
must_haves:
  truths:
    - "All 8 agent output types are importable Pydantic models from one module"
    - "TradeSignal carries audit_trail_id (UUID) and per-layer rationale"
    - "size_factor and confidence are bounded; no freetext escapes a schema field"
  artifacts:
    - path: "backend/domain/schemas/agent_schemas.py"
      provides: "TechnicalView, OnChainView, SentimentView, MacroRegime, BullCase, BearCase, RiskVerdict, TradeSignal"
      contains: "class TradeSignal"
    - path: "backend/tests/unit/domain/test_agent_schemas.py"
      provides: "Schema-validation test coverage (D-06 test 5 foundation)"
  key_links:
    - from: "backend/domain/schemas/agent_schemas.py"
      to: "pydantic.BaseModel"
      via: "all 8 classes subclass BaseModel with Field bounds"
      pattern: "class \\w+View\\(BaseModel\\)"
---

<objective>
Create the single source of truth for all agent output contracts (D-05): the new module
`backend/domain/schemas/agent_schemas.py` containing the 8 Pydantic models that every downstream
agent, the SignalDirector, the audit-trail repo, and the REST endpoint consume.

Purpose: Interface-first. Every later wave builds against these contracts, so they must exist and be
test-locked before any agent code is written. This also lays the foundation for D-06 test 5
(Pydantic schema — no freetext).

Output: `agent_schemas.py` + a schema-validation test file.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-CONTEXT.md
@backend/interfaces/rest/schemas/signals.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Define and test the 8 agent Pydantic schemas (RED→GREEN)</name>
  <read_first>
    - .planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-CONTEXT.md (D-05 full schema spec)
    - backend/interfaces/rest/schemas/signals.py (SignalVector — existing bound/disclaimer style to mirror)
    - backend/domain/schemas/steuer_schema.py (PFLICHT_DISCLAIMER constant pattern)
  </read_first>
  <behavior>
    - Test: TechnicalView requires coin, stance in {BULLISH,NEUTRAL,BEARISH}, consensus str, key_signals list, confidence in [0,1], reasoning str.
    - Test: OnChainView valuation in {CHEAP,FAIR,EXPENSIVE}, network_health in {STRONG,NEUTRAL,WEAK}.
    - Test: SentimentView score in [-1,1], regime in {FEAR,NEUTRAL,GREED}, news_surprise defaults None, veto defaults False, sources defaults [].
    - Test: MacroRegime regime in {RISK_ON,NEUTRAL,RISK_OFF}, drivers list, confidence in [0,1].
    - Test: RiskVerdict max_size in [0.0,1.5], approve bool, breaches list.
    - Test: TradeSignal action in {BUY,HOLD,SELL}, size_factor in [0,1.5], confidence in [0,1], audit_trail_id is UUID, rationale_by_layer is dict[str,str], disclaimer default non-empty German string.
    - Test (no-freetext guard / D-06 test 5): every Literal field rejects an out-of-enum value with ValidationError.
    - Test: out-of-range confidence (1.5) and negative size_factor raise ValidationError.
  </behavior>
  <action>
    Create `backend/domain/schemas/agent_schemas.py` with `from __future__ import annotations`.
    Define exactly the 8 classes from D-05 verbatim: TechnicalView, OnChainView, SentimentView,
    MacroRegime, BullCase, BearCase, RiskVerdict, TradeSignal. Use `Field(ge=..., le=...)` for all
    bounded floats (confidence 0..1, size_factor 0..1.5, score -1..1, max_size 0..1.5). Use
    `typing.Literal` for all enum fields. TradeSignal.audit_trail_id typed `uuid.UUID`.
    rationale_by_layer typed `dict[str, str]`. Default disclaimer string exactly:
    "Entscheidungsunterstützung, kein Anlagerat. Kein Auto-Trading." Do NOT add a `__post_init__`
    or any computation — these are pure data contracts. Write tests first in
    `backend/tests/unit/domain/test_agent_schemas.py` (they fail / import-error in RED), then implement.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/domain/test_agent_schemas.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from backend.domain.schemas.agent_schemas import TechnicalView, OnChainView, SentimentView, MacroRegime, BullCase, BearCase, RiskVerdict, TradeSignal"` exits 0
    - All schema tests green
    - Each Literal field rejects an invalid enum value (ValidationError asserted)
    - TradeSignal default disclaimer equals the exact German string above
  </acceptance_criteria>
  <done>All 8 schemas importable, bounds and enums enforced, schema tests pass.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/domain/test_agent_schemas.py -q` all green
- Module imports cleanly with no side effects
</verification>

<success_criteria>
- agent_schemas.py exists with all 8 D-05 models, fully bound-checked
- No field accepts freetext where D-05 specifies a Literal (D-06 test 5 foundation in place)
</success_criteria>

<output>
Create `.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-01-SUMMARY.md` when done
</output>
