---
phase: 1
plan: 01
type: feature
wave: 1
depends_on: []
files_modified:
  - backend/alembic/versions/0037_crypto_universe.py
  - backend/alembic/versions/0038_crypto_price_history_extend.py
  - backend/infrastructure/adapters/crypto_price_adapter.py
  - backend/tests/unit/infrastructure/test_crypto_price_adapter.py
autonomous: true
requirements:
  - "Seed Top-10 crypto universe in DB"
  - "OHLCV backfill since 2017 via yfinance"
  - "Coverage check: ≥200 days per coin, no gap >3 days"
---

<objective>
Create the crypto_universe table seeded with Top-10 coins and backfill OHLCV data into crypto_price_history since 2017. This is Wave 1a — the price data foundation all signal modules depend on.
</objective>

<context>
@.planning/phases/01-v4-1-signal-engine/01-CONTEXT.md
@.planning/phases/01-v4-1-signal-engine/01-RESEARCH.md
@docs/AGENTS.md
@CLAUDE.md
@backend/alembic/versions/0021_add_ml_feature_columns.py
@backend/infrastructure/adapters/yfinance_swiss.py
</context>

<tasks>
<task id="T01">
  <name>Migration 0037: crypto_universe table + seed</name>
  <files>
    - backend/alembic/versions/0037_crypto_universe.py
  </files>
  <action>
  Create Alembic migration 0037 revising 0022. In upgrade(): create table crypto_universe with columns coin_id (Integer PK autoincrement), symbol (String not null unique, e.g. "BTC-USD"), name (String not null), active (Boolean default True), added_at (Date not null). After create_table, use op.bulk_insert() to seed all 10 rows: BTC-USD/Bitcoin, ETH-USD/Ethereum, SOL-USD/Solana, BNB-USD/BNB, XRP-USD/XRP, ADA-USD/Cardano, AVAX-USD/Avalanche, DOGE-USD/Dogecoin, LINK-USD/Chainlink, DOT-USD/Polkadot. In downgrade(): op.drop_table("crypto_universe"). Check if crypto_price_history already exists (it does from migration 0001) — no schema change needed there, just use existing table with coin symbol as ticker field.
  Commit: feat(data): migration 0037 crypto_universe + seed top-10
  </action>
  <verify>alembic upgrade head runs without error; SELECT COUNT(*) FROM crypto_universe returns 10</verify>
  <acceptance_criteria>Migration applies cleanly; 10 rows in crypto_universe; downgrade removes table</acceptance_criteria>
</task>

<task id="T02">
  <name>CryptoPriceAdapter: yfinance OHLCV backfill for Top-10</name>
  <files>
    - backend/infrastructure/adapters/crypto_price_adapter.py
    - backend/tests/unit/infrastructure/test_crypto_price_adapter.py
  </files>
  <action>
  TEST-FIRST. Write test file first: mock yfinance.download, assert adapter maps columns correctly (Open→open, High→high, Low→low, Close→close, Volume→volume), assert retry on network error (max 2 retries), assert coverage check raises if fewer than 200 rows returned.

  Then implement CryptoPriceAdapter in backend/infrastructure/adapters/crypto_price_adapter.py. Class with method async fetch_ohlcv(symbol: str, start: str = "2017-01-01") -> pd.DataFrame. Use asyncio.to_thread(yfinance.download, symbol, start=start, interval="1d", progress=False). Retry pattern: _RETRIES=2, _BASE_DELAY=1.0, exponential backoff. Return DataFrame with columns [date, open, high, low, close, volume, symbol]. Symbol format: "BTC-USD" (direct pass-through, no .SW suffix). Add method validate_coverage(df: pd.DataFrame) -> None: raises ValueError if len(df) < 200 or max gap > 3 trading days (use df.index.diff().dt.days > 3 check after filtering weekdays).

  Do NOT call the live yfinance API in unit tests — mock with unittest.mock.patch("yfinance.download").
  Commit: feat(data): crypto price adapter + ohlcv backfill (yfinance)
  </action>
  <verify>pytest backend/tests/unit/infrastructure/test_crypto_price_adapter.py -v — all tests green; ruff check + mypy pass</verify>
  <acceptance_criteria>Adapter fetches OHLCV via asyncio.to_thread; retry on error; coverage validation works; all unit tests green without live network calls</acceptance_criteria>
</task>
</tasks>

<verification>
- pytest backend/tests/unit/infrastructure/test_crypto_price_adapter.py -v
- ruff check backend/infrastructure/adapters/crypto_price_adapter.py
- mypy backend/infrastructure/adapters/crypto_price_adapter.py
- alembic upgrade head (integration only)
</verification>

<success_criteria>
- Migration 0037 applies cleanly with 10 seeded coins
- CryptoPriceAdapter unit tests green (no live network)
- Adapter returns correctly shaped DataFrame with retry logic
- ruff + mypy clean
</success_criteria>
