---
phase: 03-v4-3-agentic-layer
plan: 03
type: tdd
wave: 3
depends_on: ["03-01"]
files_modified:
  - backend/application/agents/technical_analyst_agent.py
  - backend/application/agents/onchain_analyst_agent.py
  - backend/application/agents/sentiment_analyst_agent.py
  - backend/application/agents/macro_regime_agent.py
  - backend/infrastructure/llm/prompts/technical_analyst.de.md.j2
  - backend/infrastructure/llm/prompts/onchain_analyst.de.md.j2
  - backend/infrastructure/llm/prompts/sentiment_analyst.de.md.j2
  - backend/infrastructure/llm/prompts/macro_regime.de.md.j2
  - backend/tests/unit/application/test_analyst_agents.py
autonomous: true
requirements: [REQ-3.1, REQ-3.2, REQ-3.3, REQ-3.4, REQ-3.10]
must_haves:
  truths:
    - "Four analyst agents each return their Pydantic view, never inventing numbers"
    - "SentimentAnalystAgent reads real Fear&Greed from market_sentiment and normalizes (fg-50)/50"
    - "MacroRegimeAgent is a NEW Haiku agent with 1h cache; existing MacroIntelligenceAgent untouched"
    - "Each analyst falls back to a deterministic view on LLM Exception (no 500)"
  artifacts:
    - path: "backend/application/agents/technical_analyst_agent.py"
      provides: "TechnicalAnalystAgent.analyze(sub_scores) -> TechnicalView"
      contains: "class TechnicalAnalystAgent"
    - path: "backend/application/agents/sentiment_analyst_agent.py"
      provides: "SentimentAnalystAgent.analyze(coin) -> SentimentView from market_sentiment"
      contains: "class SentimentAnalystAgent"
    - path: "backend/application/agents/macro_regime_agent.py"
      provides: "MacroRegimeAgent.get_regime() -> MacroRegime (1h cache, Haiku)"
      contains: "class MacroRegimeAgent"
  key_links:
    - from: "sentiment_analyst_agent.py"
      to: "market_sentiment table"
      via: "SQLAlchemy async read of fear_greed value"
      pattern: "market_sentiment"
    - from: "technical_analyst_agent.py"
      to: "engine_signal.sub_scores"
      via: "numbers passed in, asserted equal in hallucination guard"
      pattern: "sub_scores"
---

<objective>
Build the four parallel Analyst agents (D-01 analysts, D-03, D-04) using the SteuerAgent gold-standard
pattern: constructor injection, LLM call, `json.loads(response.content[0].text)` → Pydantic validate,
deterministic fallback on Exception. TechnicalAnalyst and OnChain interpret engine/tool numbers;
Sentiment reads real Fear&Greed from `market_sentiment`; MacroRegime is a NEW Haiku agent with 1h cache.

Purpose: These four run in parallel under SignalDirector (Wave 4) via asyncio.gather. They are the
"interpret, never compute" layer enforcing the §0 iron rule.

Output: 4 agent modules + 4 Jinja2 prompts + unit tests.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-CONTEXT.md
@./CLAUDE.md
@backend/application/agents/steuer_agent.py
@backend/domain/schemas/agent_schemas.py
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: TechnicalAnalystAgent + OnChainAnalystAgent (RED→GREEN)</name>
  <read_first>
    - backend/application/agents/steuer_agent.py (constructor injection, json.loads output, fallback)
    - backend/infrastructure/llm/client.py (messages_create signature: model, messages, max_tokens, system)
    - backend/infrastructure/llm/prompts/prompt_loader.py (render() API)
    - backend/domain/schemas/agent_schemas.py (TechnicalView, OnChainView)
  </read_first>
  <behavior>
    - Test (hallucination guard / D-06 test 1): mock LLM returns a TechnicalView whose numbers equal the injected sub_scores; assert agent output number == engine number, diff < 1e-9.
    - Test: TechnicalAnalystAgent.analyze(sub_scores) returns a valid TechnicalView with stance derived from consensus.
    - Test: OnChainAnalystAgent.analyze(coin) returns valid OnChainView.
    - Test (fallback): LLM raises Exception → a deterministic OnChainView/TechnicalView is returned (lowered confidence), no exception propagates.
    - Test (Pydantic / D-06 test 5): LLM returns freetext for a Literal field → ValidationError caught → fallback used.
  </behavior>
  <action>
    Create `TechnicalAnalystAgent` and `OnChainAnalystAgent`. Constructor injects `LLMClient` and
    `PromptTemplateLoader`. Use `_MODEL = "claude-haiku-4-5-20251001"`. `analyze()` renders the
    Jinja2 system/user prompts, calls `await self._llm.messages_create(...)`, parses
    `json.loads(response.content[0].text)`, validates against TechnicalView/OnChainView. On any
    Exception or ValidationError, return a deterministic fallback view built from the input numbers
    with confidence lowered (e.g. 0.4) and reasoning noting the fallback. Create the two Jinja2
    prompt files instructing the LLM to interpret ONLY the provided numbers and emit JSON matching the
    schema — explicitly forbid inventing numbers. Tests first (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/application/test_analyst_agents.py -k "technical or onchain" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Hallucination-guard test green: output float == input float, diff < 1e-9
    - Fallback test green: Exception → valid view, no raise
    - Both agents use Haiku model constant
  </acceptance_criteria>
  <done>Technical + OnChain agents return validated views with deterministic fallback.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: SentimentAnalystAgent stub (real F&G) + MacroRegimeAgent (1h cache) (RED→GREEN)</name>
  <read_first>
    - backend/application/agents/steuer_agent.py
    - backend/domain/schemas/agent_schemas.py (SentimentView, MacroRegime)
    - 03-CONTEXT.md (D-03 tool list, D-04 normalization formula)
    - backend/application/services/universe_suggestion_service.py (only for cache/dict pattern reference if needed)
  </read_first>
  <behavior>
    - Test (D-04): SentimentAnalystAgent reads fg_value from a mocked market_sentiment query; SentimentView.score == (fg_value - 50)/50; regime FEAR/NEUTRAL/GREED matches threshold; news_surprise is None, sources == [], veto False.
    - Test: score for fg_value=50 is 0.0; fg_value=0 is -1.0; fg_value=100 is 1.0.
    - Test (D-03): MacroRegimeAgent.get_regime() returns a valid MacroRegime; uses Haiku model.
    - Test (cache / D-03): two get_regime() calls within TTL trigger only ONE LLM call (assert llm.messages_create.call_count == 1).
    - Test (fallback): LLM Exception → MacroRegime NEUTRAL with lowered confidence.
  </behavior>
  <action>
    Create `SentimentAnalystAgent` with constructor-injected async session (or sentiment repository).
    `analyze(coin)` reads the latest Fear&Greed from `market_sentiment`, computes
    `score = (fg_value - 50) / 50`, maps regime by threshold (e.g. <-0.2 FEAR, >0.2 GREED else
    NEUTRAL), sets news_surprise=None, veto=False, sources=[], reasoning referencing the fg value and
    "News-RAG: V4-4 pending". This is a stub — NO LLM call required for the numeric read (numbers come
    from DB, satisfying the iron rule). Create `macro_regime_agent.py` (NEW file — do NOT touch
    MacroIntelligenceAgent) with four tool-stub helpers (get_us_realrate, get_dxy,
    get_btc_risk_correlation, get_fear_greed) returning mock/tool data, an LLM call producing
    MacroRegime, Haiku model, and a 1h TTL module-level/functools cache (Claude's discretion per D-03).
    Fallback to NEUTRAL on Exception. Tests first.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/application/test_analyst_agents.py -k "sentiment or macro" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Sentiment score normalization exact for fg in {0,50,100} (-1.0, 0.0, 1.0)
    - Macro cache: 2 calls → 1 LLM call asserted
    - No import of / change to MacroIntelligenceAgent (grep clean)
  </acceptance_criteria>
  <done>Sentiment stub reads real F&G; MacroRegime is new, cached, Haiku, with fallback.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/application/test_analyst_agents.py -q` green
- `grep -rn "MacroIntelligenceAgent" backend/application/agents/macro_regime_agent.py` returns nothing (untouched contract)
</verification>

<success_criteria>
- Four analyst views returned, all numbers traceable to engine/DB/tool, deterministic fallback everywhere
</success_criteria>

<output>
Create `.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-03-SUMMARY.md` when done
</output>
