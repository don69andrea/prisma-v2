---
phase: 05-v4-4c-robustness-stress-test
plan: 03
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
autonomous: false
requirements:
  - D-08
  - D-11

must_haves:
  truths:
    - "docs/PRISMA_V4_FORTSCHRITT.md has a new section 'V4-4c Robustheits-Harness' appended (not replacing existing content)"
    - "Section includes 4 tables — one per stress dimension — with real numbers from running scripts/robustness_check.py"
    - "MATIC-USD documented as 'endet 2025-03-24 (umbenannt zu POL)' in the report"
    - "SOL/AVAX/DOT/MATIC Bear-2018 documented as 'n/a — erst gelistet ab [Jahr]'"
    - "Edge-Klassifikation present: one of 'robust / fragil / regime-abhängig' with written justification"
    - "User has approved the numbers before they are written to FORTSCHRITT.md (D-11)"
  artifacts:
    - path: "docs/PRISMA_V4_FORTSCHRITT.md"
      provides: "Append-only robustness report section with honest edge classification"
      contains: "V4-4c Robustheits-Harness"
  key_links:
    - from: "docs/PRISMA_V4_FORTSCHRITT.md"
      to: "scripts/robustness_check.py output"
      via: "executor runs harness, shows user tables, user approves, executor appends"
      pattern: "Edge-Klassifikation"
---

<objective>
Run scripts/robustness_check.py, show the Rich console tables to the user, and — only after user approval — append the results as a new section to docs/PRISMA_V4_FORTSCHRITT.md.

Purpose: Honest documentation of the robustness findings per D-08. The user verifies that the numbers are correct and the edge classification is honest before they are committed to the append-only log.

This plan is AUTONOMOUS=FALSE because D-11 requires the user to approve results before they enter FORTSCHRITT.md. The executor MUST NOT write to FORTSCHRITT.md without explicit user approval in this session.

Output: docs/PRISMA_V4_FORTSCHRITT.md — new section "V4-4c Robustheits-Harness" appended.
</objective>

<execution_context>
@/Users/andreapetretta/prisma-v2/.claude-2-2/get-shit-done/workflows/execute-plan.md
@/Users/andreapetretta/prisma-v2/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/andreapetretta/prisma-v2/.planning/ROADMAP.md
@/Users/andreapetretta/prisma-v2/.planning/phases/PRISMA-05-v4-4c-robustness-stress-test-planned/05-CONTEXT.md
@/Users/andreapetretta/prisma-v2/.planning/phases/PRISMA-05-v4-4c-robustness-stress-test-planned/05-01-SUMMARY.md
@/Users/andreapetretta/prisma-v2/.planning/phases/PRISMA-05-v4-4c-robustness-stress-test-planned/05-02-SUMMARY.md
@/Users/andreapetretta/prisma-v2/docs/PRISMA_V4_FORTSCHRITT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run robustness_check.py and capture all 4 dimension outputs</name>

  <read_first>
    - docs/PRISMA_V4_FORTSCHRITT.md — read current content so append doesn't duplicate; verify file ends with "---" separator
    - scripts/robustness_check.py — understand what main() returns and table structure
  </read_first>

  <action>
    Activate the venv and run the harness script from the project root:

    source /tmp/prisma-v2/venv/bin/activate && cd /Users/andreapetretta/prisma-v2 && python scripts/robustness_check.py 2>&1 | tee /tmp/robustness_output.txt

    This will print all 4 Rich tables to the terminal. The output must be captured so it can be reviewed.

    After running, confirm:
    - Dimension 1 (Kosten-Sensitivität): BTC-USD and ETH-USD results for costs=0.001, 0.002, 0.005
    - Dimension 2 (Regime-Splits): All 4 regimes across available coins; insufficient coins marked
    - Dimension 3 (Universum): All 10 coins with strategy vs baseline metrics
    - Dimension 4 (Parameter-Stabilität): SMA windows [50,75,100,150,200] for BTC-USD and ETH-USD

    If the script errors, fix the error before proceeding. Do NOT proceed to the checkpoint with error output.

    Runtime note: The full run (10 coins × 4 regimes + 2 coins × 5 windows) will download ~14 yfinance series. Estimated runtime: 2-5 minutes. Do not kill early.
  </action>

  <verify>
    <automated>test -f /tmp/robustness_output.txt && grep -c "Sharpe" /tmp/robustness_output.txt | grep -v "^0$"</automated>
  </verify>

  <done>
    - scripts/robustness_check.py ran to completion without uncaught exception
    - Output file /tmp/robustness_output.txt exists with Sharpe numbers
    - All 4 dimension tables visible in output
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Executor ran scripts/robustness_check.py and captured output. Tables for all 4 stress dimensions are visible above.
  </what-built>

  <how-to-verify>
    Review the tables shown in the terminal output above (or in /tmp/robustness_output.txt).

    Check each dimension:
    1. Kosten-Sensitivität (D-03): Are BTC-USD and ETH-USD results for 0.1%, 0.2%, 0.5% costs shown? Does Sharpe degrade with higher costs as expected?
    2. Regime-Splits (D-04): Do all 4 regimes appear? Are SOL/AVAX/DOT/MATIC Bear-2018 marked as insufficient?
    3. Universum (D-05): All 10 coins covered? MATIC-USD shows "ends 2025-03-24 (renamed POL)"?
    4. Parameter-Stabilität (D-06): All 5 SMA windows shown? Window=100 marked as Default?

    Verify honesty (D-08): Numbers should NOT be cherry-picked. If the edge is fragile in some regimes (e.g. Bear 2022 Sharpe < baseline), that is expected and must be reported honestly.

    Decide on edge classification:
    - "robust": beats baseline in ALL dimensions and ALL regimes
    - "regime-abhängig": beats baseline in bull markets but not consistently in bear markets
    - "fragil": does NOT beat baseline in most dimensions

    Then decide the correct classification based on actual numbers.
  </how-to-verify>

  <resume-signal>
    Type one of:
    - "approved — robust"
    - "approved — regime-abhängig"
    - "approved — fragil"
    - "rejected — [describe issue]"

    Include any corrections or notes you want in the report (e.g., specific numbers to highlight, edge case to explain).
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Append V4-4c section to docs/PRISMA_V4_FORTSCHRITT.md</name>

  <read_first>
    - docs/PRISMA_V4_FORTSCHRITT.md — read full current content to know exact append position (end of file)
    - /tmp/robustness_output.txt — extract actual numbers for tables
  </read_first>

  <action>
    Only execute this task AFTER the user has typed "approved" in the checkpoint above.

    Read the actual numbers from /tmp/robustness_output.txt (or from what main() returned).
    Append the following section to docs/PRISMA_V4_FORTSCHRITT.md (append-only — never modify existing content):

    SECTION FORMAT to append:

    ---

    ## V4-4c Robustheits-Harness — [DATE: today's date] (Harness: scripts/robustness_check.py)

    > Ehrliche Robustheitsprüfung des V4-1-Edge. 4 Stress-Dimensionen. Keine Parameter-Anpassung (D-01). Echte yfinance-Daten (D-02).

    ### Harness-Konfiguration

    | Parameter | Wert |
    |-----------|------|
    | Trend-MA Default | SMA(100) |
    | Vol-Schätzung | rolling(21).std() × √252 (Näherung, kein fit_walkforward) |
    | Walk-Forward | min_train=252, step=63 (unverändert, walkforward.py) |
    | Kosten Default | 0.1% RT (0.001) |
    | Annualisierung | 252 Handelstage |

    ### Dimension 1: Kosten-Sensitivität (D-03)

    [Table with actual BTC-USD and ETH-USD results across 0.001/0.002/0.005]

    | Coin | Kosten | Sharpe (Strat) | Sharpe (ExpMatch) | Calmar (Strat) | Calmar (ExpMatch) | MaxDD | Edge? |
    |------|--------|---------------|------------------|---------------|------------------|-------|-------|
    [Fill with real numbers from output]

    ### Dimension 2: Regime-Splits (D-04)

    Vollständige Preishistorie bis Regime-Ende wird für Walk-Forward genutzt; OOS-Returns auf Regime-Fenster zugeschnitten.

    | Coin | Regime | Sharpe (Strat) | Sharpe (Base) | Calmar (Strat) | OOS-Rows | Status |
    |------|--------|---------------|--------------|---------------|----------|--------|
    [Fill with real numbers; mark insufficient rows]

    **Daten-Hinweis:** SOL/AVAX/DOT/MATIC für Bear 2018 nicht verfügbar (erst gelistet 2019/2020). MATIC-USD endet 2025-03-24 (umbenannt zu POL); Bull 2023-24 bis 2024-12-31 abgedeckt.

    ### Dimension 3: Volles Universum (D-05)

    Vol-Schätzung: rolling(21)-Näherung (kein ML-Vol-Forecast) — kann minimal von V4-1-Basiszahlen abweichen.

    | Coin | Sharpe (Strat) | Sharpe (ExpMatch) | Calmar (Strat) | MaxDD | Beats Baseline? | Note |
    |------|---------------|------------------|---------------|-------|-----------------|------|
    [Fill with real numbers for all 10 coins]

    ### Dimension 4: Parameter-Stabilität — SMA-Fenster (D-06)

    Fester Ankerpunkt: SMA(100) = V4-1-Default. Alle anderen sind Varianten. Kein Tuning (D-01).

    | Coin | MA-Fenster | Default? | Sharpe | Calmar | MaxDD | Beats Baseline? |
    |------|-----------|---------|-------|-------|-------|-----------------|
    [Fill with real numbers for BTC and ETH across all 5 windows]

    ### Edge-Klassifikation (D-08)

    **Klassifikation: [robust / regime-abhängig / fragil]** (per User-Entscheid nach Prüfung)

    **Begründung:**
    [Write 3-5 sentences based on actual findings. Be honest. If Bear-2022 shows edge < baseline, say so. If parameter stability is moderate (SMA 50 vs 200 differ significantly), note that. Reference specific numbers.]

    **MATIC-USD Hinweis:** MATIC-USD (→POL) wurde am 2025-03-24 von yfinance eingestellt. POL-USD ist nicht verfügbar. Bull 2023-24 bis 2024-12-31 abgedeckt.

    ---

    Use the actual numbers from the harness output. Do NOT invent or round numbers. Copy exactly as printed.
    The edge classification must match what the user approved in the checkpoint.
  </action>

  <verify>
    <automated>grep -c "V4-4c Robustheits-Harness" /Users/andreapetretta/prisma-v2/docs/PRISMA_V4_FORTSCHRITT.md</automated>
  </verify>

  <done>
    - docs/PRISMA_V4_FORTSCHRITT.md contains "V4-4c Robustheits-Harness" section
    - Section has 4 markdown tables with real numbers (not placeholders)
    - Edge-Klassifikation is one of: robust / fragil / regime-abhängig
    - Justification paragraph present (not empty)
    - MATIC-USD note present
    - SOL/AVAX/DOT/MATIC Bear-2018 insufficient note present
    - Existing V4-1, V4-3 sections untouched (append-only)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness output → FORTSCHRITT.md | Human-verified data flows into append-only doc |
| User checkpoint | Human approves numbers before they are written |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Tampering | FORTSCHRITT.md | mitigate | Append-only (no overwrite of existing content); user checkpoint before any write (D-11) |
| T-05-03-02 | Information Disclosure | harness output | accept | Numbers are project-internal research results; no PII |
| T-05-SC | Tampering | pip installs | accept | No new packages installed |
</threat_model>

<verification>
After task 3 completes:

```
grep "V4-4c Robustheits-Harness" docs/PRISMA_V4_FORTSCHRITT.md
grep "Edge-Klassifikation" docs/PRISMA_V4_FORTSCHRITT.md
grep "MATIC-USD" docs/PRISMA_V4_FORTSCHRITT.md
grep "V4-1 Signal-Engine" docs/PRISMA_V4_FORTSCHRITT.md  # must still exist
```

All 4 greps must return output. Last grep verifies existing content was not deleted.
</verification>

<success_criteria>
- docs/PRISMA_V4_FORTSCHRITT.md has new section "V4-4c Robustheits-Harness"
- 4 markdown tables present with real harness numbers (no [placeholder] text)
- Edge-Klassifikation: robust / regime-abhängig / fragil — present and justified
- MATIC-USD note: "endet 2025-03-24 (umbenannt zu POL)"
- SOL/AVAX/DOT/MATIC Bear-2018 documented as insufficient
- Existing V4-1 and V4-3 sections still intact (append-only)
- User gave explicit "approved" signal before write (D-11)

NOTE PER D-11 (LOCKED — User-Instruktion): /gsd-complete-milestone und jeder Merge auf develop/main sind VERBOTEN bis der User explizit "OK Execute" oder "OK Merge" schreibt. Diese Phase endet mit dem Report. Kein automatischer Abschluss.
</success_criteria>

<output>
Create .planning/phases/PRISMA-05-v4-4c-robustness-stress-test-planned/05-03-SUMMARY.md when done.
</output>
