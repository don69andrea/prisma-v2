---
phase: 1
plan: 04
type: feature
wave: 2
depends_on: [01-01-PLAN.md, 01-02-PLAN.md]
files_modified:
  - backend/application/signals/factors.py
  - backend/application/signals/vol_forecast.py
  - backend/alembic/versions/0040_vol_forecast.py
  - backend/tests/unit/application/test_factors.py
  - backend/tests/unit/application/test_vol_forecast.py
autonomous: true
requirements:
  - "Cross-sectional momentum ranking"
  - "On-chain health score (MVRV-Z + active addresses)"
  - "HAR vol-forecast with walk-forward, OOS-R² > 0"
  - "LightGBM only if OOS > HAR baseline"
---

<objective>
Implement Layer 1 (factor ranking: cross-sectional momentum + on-chain health) and Layer 3 vol-forecast foundation (HAR → optional LightGBM). Runs in parallel with 01-03-PLAN.md (Wave 2b+2c).
</objective>

<context>
@.planning/phases/01-v4-1-signal-engine/01-CONTEXT.md
@.planning/phases/01-v4-1-signal-engine/01-RESEARCH.md
@docs/AGENTS.md
@CLAUDE.md
@docs/research/poc_feasibility.py
@docs/research/poc_results.txt
</context>

<tasks>
<task id="T01">
  <name>factors.py: Cross-sectional momentum + on-chain health</name>
  <files>
    - backend/application/signals/factors.py
    - backend/tests/unit/application/test_factors.py
  </files>
  <action>
  TEST-FIRST. Write tests: test_cross_sectional_momentum_ranking (create 3-coin price DataFrame, assert ranks are deterministic 1/2/3, assert higher return → lower rank number i.e. rank 1 = best), test_momentum_no_nan_with_sufficient_history (≥90 days data), test_onchain_health_score_range (output ∈ [0,1] for valid inputs), test_onchain_health_score_nan_handling (NULL mvrv_z → use only active_address component).

  Implement backend/application/signals/factors.py:
  cross_sectional_momentum(prices: pd.DataFrame, windows: list[int] = [30, 90]) -> pd.DataFrame: prices has columns = coin symbols, index = date. Compute return_Xd = prices.pct_change(X).iloc[-1] for each window. Rank across coins (rank 1 = highest return, ascending=False). Return DataFrame with coin as index, columns [momentum_rank_30d, momentum_rank_90d, composite_rank] where composite_rank = mean of window ranks.

  onchain_health_score(df_onchain: pd.DataFrame) -> pd.Series: df_onchain has columns [coin_id, date, mvrv_z, active_addresses]. Z-score normalize mvrv_z (subtract mean, divide by std) per coin history. Z-score normalize active_addresses similarly. Clip both to [-3, 3], scale to [0,1] via (z+3)/6. Composite = 0.5 * mvrv_z_norm + 0.5 * addr_norm (fallback: use available component if other is NaN). Return pd.Series indexed by coin_id, values ∈ [0,1].
  Commit: feat(signals): cross-sectional momentum + onchain health score
  </action>
  <verify>pytest backend/tests/unit/application/test_factors.py -v — all tests green; no NaN for valid inputs; ranking deterministic</verify>
  <acceptance_criteria>Momentum rank 1 = best coin; onchain_health_score ∈ [0,1]; NaN inputs handled gracefully; mypy clean</acceptance_criteria>
</task>

<task id="T02">
  <name>vol_forecast.py: HAR + optional LightGBM + migration 0040</name>
  <files>
    - backend/alembic/versions/0040_vol_forecast.py
    - backend/application/signals/vol_forecast.py
    - backend/tests/unit/application/test_vol_forecast.py
  </files>
  <action>
  TEST-FIRST. Write tests: test_har_oos_r2_positive (generate 300-bar synthetic vol series, run fit_walkforward with HAR, assert OOS-R² > 0), test_lgbm_only_when_better (mock scenario where LightGBM OOS-R² < HAR — assert model remains HAR LinearRegression), test_predict_vol_positive (output always > 0), test_no_lookahead_in_features (vol features computed with shift(1) — assert predict using only data up to t-1), test_vol_forecast_two_coins (assert OOS-R² > 0 on 2 different synthetic series per A7.4).

  Migration 0040 (revises 0039): create table vol_forecast with columns coin_id (Integer FK crypto_universe.coin_id), date (Date), horizon (Integer, e.g. 1 or 5), pred_vol (Float), realized_vol (Float nullable), model_version (String). PK (coin_id, date, horizon).

  Implement backend/application/signals/vol_forecast.py:
  Compute realized_vol(close: pd.Series, window: int = 1) -> pd.Series: log returns std × sqrt(252), rolling(window).mean() for multi-day horizons.
  build_har_features(rv: pd.Series) -> pd.DataFrame: rv_1d=rv.shift(1), rv_5d=rv.shift(1).rolling(5).mean(), rv_22d=rv.shift(1).rolling(22).mean(). CRITICAL: all features use shift(1) to enforce look-ahead-free computation.
  fit_walkforward(close: pd.DataFrame, min_train: int = 252, step: int = 63) -> dict: returns {coin: {"model": fitted_model, "oos_r2": float, "model_type": "har"|"lgbm"}}. Expanding window: train up to t, predict t+1 to t+step. Fit HAR (LinearRegression). If lgbm OOS-R² strictly > HAR OOS-R², fit LGBMRegressor(n_estimators=100, max_depth=4, learning_rate=0.05, n_jobs=1) on HAR features + vol_of_vol_5d. Store winner.
  predict_vol(close: pd.Series, model_info: dict, asof_date: date) -> float: build HAR features up to asof_date (using shift(1)), predict with stored model, clip to min=0.01 (vol always positive).
  Commit: feat(signals): walk-forward vol forecast (HAR→LightGBM), migration 0040
  </action>
  <verify>pytest backend/tests/unit/application/test_vol_forecast.py -v — OOS-R² > 0 on synthetic data for ≥2 coins; LightGBM not used when HAR is better; no look-ahead</verify>
  <acceptance_criteria>HAR OOS-R² > 0 on synthetic series; LightGBM conditional on being strictly better; all features use shift(1); pred_vol always positive; migration applies; A7.4 satisfied</acceptance_criteria>
</task>
</tasks>

<verification>
- pip install lightgbm --break-system-packages (if not done in Wave 0)
- pytest backend/tests/unit/application/test_factors.py backend/tests/unit/application/test_vol_forecast.py -v
- ruff check backend/application/signals/factors.py backend/application/signals/vol_forecast.py
- mypy backend/application/signals/factors.py backend/application/signals/vol_forecast.py
</verification>

<success_criteria>
- Momentum ranking deterministic and correct direction
- On-chain health score ∈ [0,1] with NaN fallback
- HAR vol-forecast: OOS-R² > 0 on ≥2 synthetic coin series (A7.4)
- LightGBM only trained when OOS-R² strictly better than HAR
- All features shift(1) — no look-ahead
- Migration 0040 applies cleanly
</success_criteria>
