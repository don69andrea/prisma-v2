---
phase: 03-v4-3-agentic-layer
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/alembic/versions/0041_agent_audit_trail.py
  - backend/infrastructure/persistence/models/agent_audit_trail.py
  - backend/infrastructure/persistence/repositories/agent_audit_trail_repository.py
  - backend/tests/unit/infrastructure/test_agent_audit_trail_repository.py
autonomous: true
requirements: [REQ-3.8, REQ-3.10]
must_haves:
  truths:
    - "agent_audit_trail table is created by migration 0041 (chained on 0040)"
    - "Repository inserts a row and returns its UUID; no update/delete method exists"
    - "Two insert() calls produce two distinct rows (append-only contract, D-02)"
  artifacts:
    - path: "backend/alembic/versions/0041_agent_audit_trail.py"
      provides: "agent_audit_trail DDL (id UUID PK, coin, asof DATE, agent_run JSONB, created_at)"
      contains: "agent_audit_trail"
    - path: "backend/infrastructure/persistence/models/agent_audit_trail.py"
      provides: "AgentAuditTrailORM"
      contains: "class AgentAuditTrailORM"
    - path: "backend/infrastructure/persistence/repositories/agent_audit_trail_repository.py"
      provides: "AgentAuditTrailRepository.insert(coin, asof, agent_run) -> UUID"
      contains: "class AgentAuditTrailRepository"
  key_links:
    - from: "agent_audit_trail_repository.py"
      to: "AgentAuditTrailORM"
      via: "session.add + flush; returns generated id"
      pattern: "AgentAuditTrailORM"
---

<objective>
Persist the immutable audit trail (D-02): Alembic migration 0041, the `AgentAuditTrailORM` model, and
an append-only repository that exposes ONLY `insert()` (no update, no delete). This locks the
append-only contract with a test that asserts two inserts create two rows.

Purpose: SignalDirector (Wave 4) needs a place to write every agent run and obtain the
`audit_trail_id` it embeds in `TradeSignal`. Immutability is a compliance requirement, not a nicety.

Output: migration + ORM + repository + repository tests.
</objective>

<execution_context>
@$HOME/.claude-2-2/get-shit-done/workflows/execute-plan.md
@$HOME/.claude-2-2/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-CONTEXT.md
@backend/infrastructure/persistence/models/backtest_result.py
@backend/alembic/versions/0040_vol_forecast.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 0041 + AgentAuditTrailORM model</name>
  <read_first>
    - backend/alembic/versions/0040_vol_forecast.py (revision chaining, op.create_table style)
    - backend/infrastructure/persistence/models/backtest_result.py (UUID PK + JSONB column style)
    - backend/infrastructure/persistence/base.py (Base import path)
  </read_first>
  <action>
    Create `backend/alembic/versions/0041_agent_audit_trail.py` with `revision="0041"`,
    `down_revision="0040"`. upgrade() creates table `agent_audit_trail` with columns: `id` UUID PK
    (PostgreSQL `server_default=sa.text("gen_random_uuid()")`), `coin` String not null, `asof` Date
    not null, `agent_run` JSONB not null, `created_at` DateTime(timezone=True) not null
    server_default now(). downgrade() drops the table. Add a table-level comment noting append-only
    / no UPDATE / no DELETE (per D-02; DB trigger optional — comment is sufficient for SQLite tests).
    Create `backend/infrastructure/persistence/models/agent_audit_trail.py` with `AgentAuditTrailORM`
    mirroring backtest_result.py imports (`from sqlalchemy.dialects.postgresql import JSONB, UUID`),
    `default=uuid.uuid4` on the Python side so SQLite unit tests generate IDs.
  </action>
  <verify>
    <automated>cd backend && python -c "import importlib.util,glob; importlib.util.spec_from_file_location('m', glob.glob('alembic/versions/0041*.py')[0])" && python -c "from backend.infrastructure.persistence.models.agent_audit_trail import AgentAuditTrailORM; print(AgentAuditTrailORM.__tablename__)"</automated>
  </verify>
  <acceptance_criteria>
    - 0041 migration module imports without error and has revision "0041", down_revision "0040"
    - `AgentAuditTrailORM.__tablename__ == "agent_audit_trail"`
    - Columns present: id (UUID PK), coin, asof, agent_run (JSONB), created_at
  </acceptance_criteria>
  <done>Migration 0041 and ORM model exist and import; schema matches D-02.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: AgentAuditTrailRepository — insert-only + append-only test (RED→GREEN)</name>
  <read_first>
    - backend/infrastructure/persistence/repositories/backtest_result_repository.py
    - backend/infrastructure/persistence/repositories/decision_audit_repository.py
    - backend/tests/unit/infrastructure/ (existing repo test fixtures / in-memory session setup)
  </read_first>
  <behavior>
    - Test (D-02 append-only): calling insert() twice with same coin/asof creates 2 distinct rows; query count == 2 and the two returned UUIDs differ.
    - Test: insert() returns the row's UUID.
    - Test: repository class exposes no `update`, `delete`, or `save`-overwrite method (assert via `hasattr` / dir()).
  </behavior>
  <action>
    Create `AgentAuditTrailRepository` taking an `AsyncSession` via constructor. Implement ONLY
    `async def insert(self, coin: str, asof: date, agent_run: dict) -> uuid.UUID`: build
    AgentAuditTrailORM, `session.add`, `await session.flush()`, return `orm.id`. Do NOT add update or
    delete methods. Write tests first against an in-memory async SQLite session (reuse existing test
    fixture). RED: tests fail (repo missing). GREEN: implement insert.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/unit/infrastructure/test_agent_audit_trail_repository.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - Two inserts → row count 2, distinct UUIDs (test green)
    - No `update`/`delete` attribute on the repository (asserted)
    - insert() returns a uuid.UUID
  </acceptance_criteria>
  <done>Append-only repository tested and green; immutability contract locked.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/unit/infrastructure/test_agent_audit_trail_repository.py -q` green
- Migration chain valid: 0040 → 0041
</verification>

<success_criteria>
- agent_audit_trail persists; two inserts = two rows; no mutation path exists (D-02 enforced)
</success_criteria>

<output>
Create `.planning/phases/PRISMA-03-v4-3-agentic-layer-planned/03-02-SUMMARY.md` when done
</output>
